"""Metacognitive calibration eval — source package.

Modules:
    metrics       pure-numpy calibration metrics (ECE, Brier, AUROC, ...)
    elicitation   confidence-elicitation prompts, parsers, Inspect solvers
    scoring       correctness helpers + the combined confidence scorer
    tasks         Inspect task definitions (custom set + public anchors)
    plotting      reliability diagrams and the RQ2 domain figure
    analyze       read Inspect logs -> per-(model, domain) metrics + figures
"""
