# Python envs / caches
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/

# Run outputs: keep the dir + summaries, drop raw logs
runs/*
!runs/.gitkeep
!runs/summary.csv
!runs/reasoning_effort_probe.csv
!runs/rq1_crossfamily.csv

# Inspect default log location if used inside the experiment
logs/

# Secrets
.env
