# f-084 probe — fd-provenance-drift (release-lifecycle × manuscript-transmission)

**CONFIRMED** — all three legs of f-084 verified against source and live state on both machines; scope refined (GitHub-as-distribution is intentional for curl-pipe installs, accidental-as-default when the canonical Sylveste tree is present). f-084 overlaps upheld f-085 (installer self-transmission) and f-052 (dead refresh automation); the new material is the drift-check exemplar leg and the first probe of zklw.

SEVERITY | fd-provenance-drift | path:line | finding [t]

P2 | fd-provenance-drift | os/Clavain/scripts/codex-auto-refresh.sh:18-19 | Auto-refresh loop's exemplar defaults are the GitHub clone: `CLAVAIN_DIR=$HOME/.codex/clavain`, `CLAVAIN_REPO_URL=https://github.com/mistakeknot/Clavain.git`; it `git pull --ff-only`s that clone from GitHub origin (:76) and then executes the installer FROM that clone — `bash "$CLAVAIN_DIR/scripts/install-codex-interverse.sh" install --source "$CLAVAIN_DIR"` (:46-47, :51-52) — so the transmission machinery for the whole fleet runs from the GitHub snapshot, never from Sylveste. Env overrides exist (`CLAVAIN_DIR`, `CLAVAIN_REPO_URL`) but no automation or doc sets them to the Sylveste tree. [t]

P2 | fd-provenance-drift | os/Clavain/scripts/install-codex-interverse.sh:322-324 | `plugin_repo_url()` hardcodes the per-plugin GitHub template `https://github.com/mistakeknot/%s.git`; `ensure_repo()` (:898-915) clones/pulls every companion into `$CLONE_ROOT/$plugin` (default `~/.codex/<plugin>`, :24) from GitHub with ff-only pull. There is NO code path that prefers the local `Sylveste/interverse/<plugin>` nested checkouts (71 present, each its own git repo, e.g. `interverse/interlock/.git`) — the canonical tree is invisible to the installer. [t]

P2 | fd-provenance-drift | os/Clavain/scripts/install-codex.sh:16 | `REPO_URL_DEFAULT="https://github.com/mistakeknot/Clavain.git"` (also :25, clone at :176) — same GitHub-default exemplar for the Codex-only install path. [t]

P2 | fd-provenance-drift | os/Clavain/scripts/check-install-updates.sh:150-158 | Drift checks compare every repo against its own GitHub origin: `repo_remote_diff_status()` resolves `@{upstream}`/origin HEAD via `git ls-remote origin` — for `~/.codex/<plugin>` snapshots that means GitHub, never the canonical `Sylveste/interverse/<plugin>` checkout. Light mode (:273-319) inspects only Sylveste + ~/.codex/clavain; full mode (:307-318) adds companions but still vs GitHub origin. No check anywhere answers "is the installed snapshot behind the Sylveste tree on THIS machine". [t]

P3 | fd-provenance-drift | install.sh:95,505,595,655,700 | Sylveste's own installer is the exception that proves the rule: `find_local_clavain_source()` (:95, used :581) prefers the local checkout, and the GitHub clones (:505 intercore, :595 Clavain, :655/:700 Sylveste for Gemini/Kimi) are curl-pipe fallbacks only — evidence the GitHub default is a deliberate distribution channel, misapplied as the standing exemplar by the refresh/drift machinery above. [t]

P2 | fd-provenance-drift | zklw:~/.agents/skills → ~/.codex/<plugin> | zklw (first probe) replicates the Mac pattern: 46 skill links, all sampled ones resolve to `~/.codex/<plugin>/skills/...` snapshot clones (e.g. `interlock` snapshot last commit 2026-07-04, a month stale); `~/.codex/clavain` does not exist; the `clavain` skill instead points to a standalone `~/projects/Clavain` GitHub clone (2026-08-01) — a THIRD Clavain lineage alongside Sylveste/os/Clavain and ~/.codex/clavain, with nothing declaring which is canonical. No crontab/systemd timer for codex-auto-refresh there either. [t]

P3 | fd-provenance-drift | git remotes (both machines) | Sylveste's own origin is GitHub on BOTH Mac (https) and zklw (ssh), and both sit at the same 2026-08-05 commit — so for the Sylveste repo itself, GitHub is the de-facto git transport between the two machines and "zklw canonical" holds only socially (zklw often ahead, auto-sync), not in the remote topology. The doctrine contradiction is confined to the plugin-install layer, not Sylveste sync. [t]
