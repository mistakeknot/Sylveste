# Findings Index — fd-canonization-safety (probe 3, round 2)

Fusion: fd-kernel-contract × fd-ecosystem-consolidation. Target: os/Clavain, core/intercore, interverse.
Hard constraint honored: every finding below requires BOTH parent perspectives; each carries an intersection_justification. Settled clusters (f-029, f-038, f-086-as-orphan-fact, f-039, f-043, f-084) not re-litigated — f-086 is used only as the established fact base for the *re-attachment mechanism* question (review area 2), which the charter explicitly assigned to this probe.

Format: `SEVERITY | fd-canonization-safety | path:line | finding [t]`

---

P1 | fd-canonization-safety | core/intercore/test-integration.sh:1383 + core/intercore/lib-intercore.sh:9 + os/Clavain/hooks/lib-intercore.sh:9 | The lib-intercore.sh twin guard is double-blind: both copies stamp `INTERCORE_WRAPPER_VERSION="1.1.0"` over 266 diff lines, and the only sync check points at `$SCRIPT_DIR/../../hub/clavain/hooks/lib-intercore.sh` — a path that does not exist since the hub/clavain → os/Clavain move — so the `if [[ -f "$CLAVAIN_LIB" ]]` block (lines 1384–1392) never executes, and even if the path were fixed it compares only the version string, which is identical. [consumer census: all 9 runtime hooks (session-start.sh:557, session-handoff.sh:20, session-end-handoff.sh:29, catalog-reminder.sh:22, lib-sprint.sh:13, lib-recovery.sh:18, auto-stop-actions.sh:25, lib-dispatch.sh:19, plus lib-goal-audit.sh via callers) source the Clavain copy; nothing outside core/intercore's own test-integration.sh (lines 125, 908) ever executes the core copy — the "source of truth" template is the copy nobody runs]
  intersection_justification: Consolidation alone sees two files to merge and picks the repo-local "source" (core) as canonical — the wrong one. Contract analysis alone sees each hook sourcing a relative sibling path and finds each call site internally consistent. Only the intersection reveals that the executed contract (Clavain copy, 3-arg `intercore_sentinel_check_or_legacy`, no legacy temp-file fallback post-E3-cutover) and the archived template (core copy, 4-arg signature with `$legacy_path`, legacy fallback retained per MIGRATION.md:33) have diverged in opposite directions per function — sentinel checks fail open in Clavain (`return 0` = allow, os/Clavain/hooks/lib-intercore.sh:63-92) but fall back to temp files in core (core/intercore/lib-intercore.sh:54-96), while state ops propagate errors in Clavain (`return 1`) but swallow them in core (`return 0`) — so "canonize either copy wholesale" flips failure direction for one function class or the other in all 9 hooks, and the guard that should pin this observes nothing.

P2 | fd-canonization-safety | scripts/sync-secret-scan-baseline.sh:55,82-99 + docs/plans/2026-03-26-sylveste-migration-plan.md:39 | The marker string serves two contracts at once — provenance stamp and overwrite-protection key — and the rename migration was declared complete (`[x] 45 .gitleaks.toml files`) while 37/46 deployed `.gitleaks.toml` and 37/47 `validate-gitleaks-waivers.sh` copies still carry `# demarch-managed` (verified by md5 census: validators split 37×`33b2901b…` demarch vs 10×`882585e0…` sylveste; diff between lineages is the marker line only). `is_managed_file` greps only the new marker, so the 37 are SKIPped forever, and because the plan says done, no one re-runs the rename. [t]
  intersection_justification: Contract analysis alone concludes the SKIP branch is the protection contract working exactly as specified (never overwrite an unmanaged file). Consolidation alone sees byte-identical files and says "just add the old marker to the grep." Only the intersection exposes that the naive consolidation (grep both markers) destroys the protection semantics — any repo that deliberately pinned the old template would be stomped — while the naive contract reading preserves a fleet split the migration plan falsely claims is closed. The safe re-attachment mechanism neither parent finds alone: a historical-template-hash allowlist (a deployed file is re-attachable iff its hash matches any published template version — today provably true for all 37, since the only delta is the marker/title lines), which re-stamps marker-only drift while still SKIPping any copy with real local edits. A `v1→v2` migration stamp in the template header would make this explicit going forward.

P2 | fd-canonization-safety | scripts/test-compact-freshness.sh:21-36 + os/Clavain/skills/*/SKILL.md (compact directive) | SKILL-compact.md is the executed contract and SKILL.md is the edited source — the HTML-comment directive (`<!-- compact: SKILL-compact.md — if it exists in this directory, load it instead … -->`, present in 10 Clavain SKILL.md files) instructs the agent to load the compact copy unconditionally, while humans edit SKILL.md. Drift is live right now: 13/15 Clavain pairs have SKILL.md newer than the compact (up to 126 days: file-todos, galiana, lane, using-tmux-for-interactive-commands), the hash guard confirms 10 stale compacts (`Source hashes differ from manifest` ×10), and 6 pairs lack `.skill-compact-manifest.json` entirely. The guard's own registry is stale in three ways: it tests `interverse/interflux/skills/flux-drive` whose compact was *deliberately deleted* in the B4 canonization (bead sylveste-s46z: compact silently dropped Phase 2.5 reaction orchestration — the one completed consolidation of this exact pattern), plus phantom entries `os/clavain/skills/interserve` and `os/clavain/skills/brainstorming` that don't exist. [t]
  intersection_justification: Consolidation alone classifies the compact as a derived artifact — "just regenerate" — and misses that regeneration codifies whatever SKILL.md says *now* into the copy agents actually execute, with no fidelity check (guard tests hashes, not semantics; interflux's Phase-2.5 omission passed the guard). Contract analysis alone sees agents successfully loading a compact file and finds the load path working. Only the intersection catches the full trap: the ecosystem already paid for proof that this pair pattern silently drops orchestration semantics (4-agent convergence, B4), resolved it by single-sourcing to SKILL.md — and the fleet-wide guard still demands the deleted file's re-creation, meaning the consolidation guard and the consolidation decision contradict each other. Where the guard should live: not a hand-maintained KNOWN_SKILLS list in a root script, but per-plugin structural tests (interflux's model), with the root guard reduced to checking that each plugin *has* a freshness check.

P3 | fd-canonization-safety | scripts/session-freshness-gate.sh:50 + scripts/backfill-philosophy-protocol.py:15-46 | The ~60 drifted doctrine blocks (f-003 census: 60 PHILOSOPHY.md files carry the Brainstorming/Planning Doctrine stamp; the AGENTS.md "Philosophy Alignment Protocol" variant is in only 6 files) are cited by NO gate/hook as text — verified: the only non-doc reference is a transcript artifact. But PHILOSOPHY.md and AGENTS.md content is a hash input to session-freshness-gate.sh's orientation manifest (line 50, ORIENTATION_FILES), whose exit code IS a behavior contract (0 = skip orientation, 1 = run full orientation; `.claude/session-state.json:7` holds the live hash of the root PHILOSOPHY.md). A doctrine-canonization sweep across the fleet flips every wired repo's gate to "stale," forcing mass re-orientation sessions that are indistinguishable from real orientation drift. Separately, the backfill script's TARGETS registry is stale (`interverse/interserve` doesn't exist; `os/clavain` lowercase breaks on case-sensitive zklw). [t]
  intersection_justification: Consolidation alone sees inert doctrine text and schedules a harmless merge sweep. Contract analysis alone checks "does any gate parse this text?" — answer no — and also clears it. Only the intersection catches that the file's *content hash*, not its text, is the load-bearing input: canonizing "informational" doctrine changes a behavioral signal (gate exit code → session-start token cost and orientation behavior) across ~60 repos at once, so the sweep must either be staged or paired with a session-state manifest reset. Neither parent alone has any reason to look at the hash consumer.

P3 | fd-canonization-safety | core/marketplace/.claude-plugin/marketplace.json + scripts/interverse_inventory.py:38-97 | Three registries answer "what is the fleet" and install machinery for different hosts reads different ones: Claude Code reads marketplace.json (66 hand-listed plugins; 8 on-disk plugin dirs unlisted — interboxd, intercept, intergraph, interscout, intersense, intersite, lattice, _shared — and `interjawn` listed but absent from disk); profile packs read DEFAULT_PROFILE_PLUGINS/PROFILE_NAME_OVERRIDES embedded in the inventory *generator* (28 of 71 interverse dirs unlisted → classified by keyword heuristic over description text, interverse_inventory.py:499-511, e.g. any description containing "map" lands in observability); and the Kimi/Gemini install generators (gen-kimi-manifests.py:339-346, gen-gemini-commands.sh:16) bypass both registries and discover the filesystem dynamically — so Kimi/Gemini users can install plugins Claude Code users cannot see, and Claude Code offers interjawn, which 404s. [t]
  intersection_justification: Consolidation alone sees three overlapping lists and says "pick one." Contract analysis alone traces each consumer to its own list and finds each path self-consistent — kimi install works, marketplace install works, profile packs work. Only the intersection reveals that there is no declared canonical fleet definition, so choosing one registry to consolidate onto is a semantic decision about which host's install surface is authoritative — and that check-rig-drift.sh (the existing guard) validates inventory↔marketplace↔rig consistency but not marketplace↔disk, letting phantom entry interjawn and the 8 unlisted plugins pass. The registry that install machinery *actually* reads depends on the host: marketplace.json for Claude Code, the filesystem for Kimi/Gemini, the embedded overrides for profile packs.

P3 | fd-canonization-safety | core/intercore/lib-intercore.sh:2,9 + core/intercore/AGENTS.md:37 + core/intercore/agents/cli-reference.md:10 | Version-stamp schizophrenia in the twin library: the header comment says "Version: 0.1.0 (source: infra/intercore/lib-intercore.sh)" — a provenance path that no longer exists — while the machine-readable `INTERCORE_WRAPPER_VERSION="1.1.0"` says 1.1.0, historical docs cite v0.4.0/v0.6.0, AGENTS.md claims "44 functions" and cli-reference.md "45 functions" against an actual surface that differs between the twins (Clavain copy adds `intercore_run_create/run_list/run_status/run_agent_list/run_agent_update`). [t]
  intersection_justification: Consolidation alone sees stale comments and function-count docs to tidy. Contract analysis alone sees a versioned wrapper and checks the machine-readable stamp — which is fine in isolation at each site. Only the intersection catches that there are three competing version witnesses (provenance comment, machine stamp, doc citations) and two competing surface-size witnesses, so a consumer pinning "the contract version" cannot tell which string IS the version, and any canonization that bumps only one witness re-creates the ambiguity that made the 1.1.0/266-diff-lines split invisible in the first place.

---

Verdict summary: 6 findings (1×P1, 2×P2, 3×P3). Not a dry run — the intersection is richest exactly where the charter predicted: the lib-intercore.sh twins, where canonization is currently double-blind.
