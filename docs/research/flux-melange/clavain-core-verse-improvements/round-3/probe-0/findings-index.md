# PROBE-DISAGREEMENT Findings — Compact-Drift Guard (f-008 vs f-122)

Adjudication date: 2026-08-06. Scope: Clavain, intercore, interverse plugins under `/Users/sma/projects/Sylveste`.

```
SEVERITY | adjudicator | path | finding [t]
```

- HIGH | adjudicator | scripts/test-compact-freshness.sh:21-36 + scripts/gen-skill-compact.sh:27-42 | Root compact-drift guard exists but its hardcoded `KNOWN_SKILLS` registry (14 entries) contains 3 phantom paths: `interverse/interflux/skills/flux-drive` (dir renamed to `flux-engine`, interflux@18d393e), `os/clavain/skills/interserve` (renamed `interserve-engine`, clavain@6b352a9), `os/clavain/skills/brainstorming` (zero git history in os/clavain — never existed there). Guard therefore always exits non-zero and is useless as a signal. [t]
- HIGH | adjudicator | os/clavain/skills/*/SKILL-compact.md | All 15 Clavain SKILL.md/SKILL-compact.md pairs currently fail the guard's own freshness check: 9 genuine source-hash drift (code-review-discipline, dispatching-parallel-agents, engineering-docs, executing-plans, file-todos, interserve-engine, landing-a-change, subagent-driven-development, writing-plans) + 6 missing `.skill-compact-manifest.json` (galiana, lane, project-onboard, refactor-safely, upstream-sync-engine, using-tmux-for-interactive-commands). Drift age up to 126 days (using-tmux, lane, galiana, file-todos) — round-2's "13/15, up to 126d" is now 15/15. [t]
- HIGH | adjudicator | .github/workflows/ + os/clavain/.github/workflows/ + scripts/pre-commit-hook.sh | Guard is wired nowhere: no root CI workflow mentions compact (root runs only skill-listing-budget, kimi-manifest-drift, etc.), Clavain's 7 workflows (test.yml, eval-*.yml, ...) never invoke it, pre-commit hook doesn't either. Broken guard + zero enforcement = compacts drift silently. [t]
- MEDIUM | adjudicator | scripts/gen-skill-compact.sh:27-42 | Registry is incomplete even where dirs exist: 7 live pairs are uncovered (flux-engine, galiana, interserve-engine-under-old-name, lane, project-onboard, refactor-safely, upstream-sync-engine, using-tmux-for-interactive-commands). A root-level hardcoded list cannot keep pace with per-plugin renames — structural flaw, not a one-time oversight. [t]
- MEDIUM | adjudicator | interverse/interflux/hooks/check-compact-drift.sh | interflux's per-plugin drift hook was deliberately deleted in the canonization commit d2a1ded ("dead scaffolding once dual-file drift is impossible"), then resurrected onto main via b5f537c ("rescue uncommitted deliverables") — but it is NOT registered in hooks/hooks.json, so it is inert. f-008's "a guard exists only in interflux" is stale: interflux's guard is a dead file. [t]
- MEDIUM | adjudicator | interverse/interflux/skills/flux-engine/SKILL-compact.md | The rename commit 18d393e silently re-created a 310-line SKILL-compact.md for flux-engine — directly contradicting d2a1ded's deliberate canonization to a single SKILL.md (motivated by the compact silently dropping Phase 2.5 reaction-round orchestration). The re-created compact has no compact-mode preamble in SKILL.md and is itself stale per the checker. Canonization was half-executed and then partially reverted by accident. [t]
- LOW | adjudicator | scripts/test-compact-freshness.sh:106-111 | Freshness = source-file hash equality with manifest. Any SKILL.md edit (even a typo fix) marks the compact stale without evidence the compact's *content* is wrong — maximizes false-positive pressure and trains maintainers to ignore the guard. [t]
- INFO | adjudicator | (verdict) | f-008: EXISTENCE-claim fails (a root guard exists and even lists 8 Clavain skills), EFFECT-claim stands (no functioning guard protects Clavain's pairs; interflux's is dead). f-122: fully verified — guard exists, registry stale, 1 canonization-contradicting entry (flux-drive) + 2 further phantoms (interserve, brainstorming), and it demands re-creating a file whose deletion was a deliberate, documented decision. [t]
