# Round 3 / Probe 1 — fd-kernel-contract findings index

P1 | fd-kernel-contract | core/intercore/internal/lifecycle/lifecycle.go:374 + core/intercore/internal/dispatch/collect.go:37 | f-044 CONFIRMED: kernel has no serving-path stall detection — the lifecycle stall detector (CheckStall/CheckStalls) is dead code with zero production callers, TimeoutSec is only enforced while a caller actively runs `ic dispatch wait --timeout` (cmd/ic/dispatch.go:299), Poll returns non-terminal dispatches with nil PID unchanged forever (collect.go:37-39), and no reaper exists for dispatches stuck spawned/running
P1 | fd-kernel-contract | core/intercore/cmd/ic/run_replay.go:79 + core/intercore/cmd/ic/run_replay.go:115 | f-045 CONFIRMED: kernel replay is recovery-inert — `ic run replay` refuses any run not in status=completed (line 79-82, exactly the crashed/stalled runs recovery is for) and reexecute mode is a hardcoded stub ("currently disallowed by kernel policy", line 115) that always exits 1 (line 135-137); internal hook/spawn handlers fire only synchronously in the advancing process with no handler-execution cursor, so a crash between event insert and handler completion loses the handler effect with no replay path (cmd/ic/run_lifecycle.go:195-206)
P1 | fd-kernel-contract | core/intercore/cmd/ic/events.go:126 + core/intercore/cmd/ic/events.go:128 | events tail hardcodes the coordination cursor to 0 in every query (ListAllEvents/ListEvents 4th arg) and the high-water-mark loop (events.go:143-154) has no coordination case — coordination events are redelivered in every poll batch to `--follow` consumers and on every restart regardless of cursor, while the saved/loaded "interspect" cursor field (loadCursor events.go:712-733) is never used in any query at all
P1 | fd-kernel-contract | core/intercore/cmd/ic/dispatch.go:102 + core/intercore/internal/scheduler/scheduler.go:144 | scheduled-dispatch path is a write-only queue: `ic dispatch spawn --scheduled` and `ic scheduler submit` persist scheduler_jobs rows and return success, but scheduler.New/Start has no production caller and no `ic scheduler run` subcommand exists (cmd/ic/scheduler_cmd.go:21-41) — jobs sit pending forever; Store.RecoverPending (internal/scheduler/store.go:110) is also never called, so even a future runner has no crash recovery wired
P1 | fd-kernel-contract | core/intercore/internal/event/handler_spawn.go:46 + core/intercore/cmd/ic/run_lifecycle.go:205 | auto-spawn failure is doubly invisible: the spawn handler swallows per-agent spawn errors with a Warn log and returns nil (handler_spawn.go:46-51), and run advance discards Notify's error return (run_lifecycle.go:135,205) — advance exits 0, run sits in "executing" with no agent, no failure event recorded, nothing retries; this is the concrete stall window behind f-044
P1 | fd-kernel-contract | core/intercore/internal/portfolio/relay.go:238 | relay queryChildEvents has no run_id filter (relay.go:238-241) — it relays the child DB's entire phase_events history across all runs into the portfolio run's phase_events; a missing cursor (new child, expired/deleted state key) replays full child history, durably contaminating the portfolio run's event stream with events from unrelated historical runs
P2 | fd-kernel-contract | core/intercore/internal/portfolio/relay.go:87 | mapChildEventType collapses every unknown child event type to child_advanced via the default branch (relay.go:87-98) — gate failures, custom events, or any future child event type silently become "advanced", and relayed rows carry child from/to phases but run_id=portfolioID (relay.go:177-182), so portfolio consumers cannot distinguish real portfolio transitions from relayed child noise
P2 | fd-kernel-contract | core/intercore/cmd/ic/dispatch.go:706 | portfolio dispatch limit silently disables when the relay is down: checkPortfolioDispatchLimit "degrades gracefully" to not-enforced with only a slog.Warn when the relay-maintained active-dispatch-count key is missing/stale (dispatch.go:706-710) — same inert-enforcement pattern as the settled nil-recorder budget finding
P2 | fd-kernel-contract | core/intercore/internal/dispatch/collect.go:46 + core/intercore/MIGRATION.md:77 | MIGRATION.md landmine: the kernel's own live-stats path still reads /tmp/clavain-dispatch-<pid>.json (collect.go:46), so Phase 3 cleanup (rm /tmp/clavain-dispatch-*.json, MIGRATION.md:77) silently freezes turns/commands/messages updates — Poll swallows the read error (collect.go:47); `ic compat status` will perpetually report dispatch as LEGACY=yes because the kernel itself is the legacy writer
P2 | fd-kernel-contract | core/intercore/cmd/ic/events.go:753 | events tail cursor TTL defaults to 24h for non-durable consumers (cursorTTL, events.go:749-754) — a consumer down >24h loses its cursor silently and restarts at 0, triggering a full-history replay into possibly non-idempotent consumers with no warning or machine-readable signal
P3 | fd-kernel-contract | core/intercore/internal/audit/audit.go:214 | audit chain refines the "write-only" suspicion to "never written, never verified": VerifyIntegrity has no production caller, and audit.New/Logger.Log are never invoked anywhere in cmd/ or internal/ outside audit_test.go — the audit_log table (internal/db/schema.sql:366) is dead schema and the whole tamper-evident package is dead code
P3 | fd-kernel-contract | core/intercore/internal/event/replay_capture.go:33 | replay input capture silently no-ops when run_replay_inputs is missing or the FK fails (replay_capture.go:33-36) — replay fidelity degrades with no error, so `ic run replay inputs` can under-report without anyone knowing
P3 | fd-kernel-contract | core/intercore/internal/replay/reconstruct.go:36 | BuildTimeline drops coordination, review, discovery, and agency events from the "deterministic" timeline (reconstruct.go:36 keeps only phase+dispatch sources) — lock acquisitions and gate-adjacent events that influenced decisions are absent from the replay record
