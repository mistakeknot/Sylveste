PARTIAL (downgrade P0 → P2) | fd-kernel-contract | Sylveste/docs/handoffs/2026-04-21-sylveste-ynh7-post-txky.md:59 | f-017: docs/beads prescribing raw `DELETE FROM publish_state` are real and never migrated (CONFIRMED half), but every production-use data point predates `ic publish unlock` (added 2026-06-19/22, commits 5170fa0 + c4e9b2a) and the kernel now self-heals the common stuck states — failed rows are deleted on all failure paths (engine.go:217-224, sylveste-2uhz closed 2026-06-22) and stale rows (error recorded, or >1h inactive, publish.go:121-126 IsStale) are auto-cleared on the next plain `ic publish` (engine.go:167-184) — so hand SQL is no longer the de-facto recovery, just unmigrated historical documentation (REFUTED half). No post-June-2026 bead or doc shows a hand DELETE actually executed.
P2 | fd-kernel-contract | interverse/interpub/.claude-plugin/commands/release.md:53 | Live consumer-facing doc still says "Active publish in progress … Re-run to force" — re-run never forced anything (the old error text was admitted-misleading in sylveste-2uhz) and the line omits `ic publish unlock`, so the one doc agents actually read during a stuck publish teaches a nonexistent recovery. [t]
P2 | fd-kernel-contract | Sylveste/docs/handoffs/2026-05-14-deepseek-v4-spike-kickoff.md:37 | Points agents at an out-of-repo recipe file `~/.claude/projects/-Users-sma-projects-solwend/memory/reference_intercore_publish_stuck_locks.md` prescribing row deletion in `~/.clavain/intercore.db`; agent memory keeps resurfacing the bypass long after the kernel fix, and it is invisible to repo-scoped doc sweeps. [t]
P2 | fd-kernel-contract | Sylveste/.beads/issues.jsonl (Sylveste-46v, open) | Open bead's premise — "publish flow has no auto-recovery … build an 'ic publish unstick' subcommand" — is satisfied (unlock + IsStale auto-clear shipped June 2026) but the bead was never revisited; open stale beads keep prescribing already-built work.
P1 | fd-kernel-contract | core/intercore/internal/publish/state.go:163-179 | `ClearLocks` (backing `ic publish unlock`) deletes every non-done/idle row with no IsStale guard, no --force, no --dry-run, and no confirmation — it can stomp a genuinely in-flight publish in another terminal exactly like the raw DELETE it replaces, and unlike the engine path (engine.go:169) it never consults the staleness predicate. The CLI recovery path replicates the hazard of the bypass.
P2 | fd-kernel-contract | core/intercore/cmd/ic/publish.go:482-497 | `ic publish unlock` scopes by cwd plugin root or --all — there is no `ic publish unlock <plugin>` by name, so clearing another plugin's lock from outside its directory (a documented multi-DB scenario, handoff 2026-04-21:60) still pushes operators toward raw SQL; unlock also silently targets only the single DB from resolveDBPath while stuck rows can live in either the monorepo or plugin-local `.clavain/intercore.db`.
P3 | fd-kernel-contract | Sylveste/docs/research/2026-04-28-sylveste-zppj-results.md:65-68 | Historical research doc embeds the sqlite3 DELETE recipe; its own follow-up recommendation ("expose `ic publish unlock <plugin>` as a first-class command") is now shipped, so the doc is internally contradicted by the kernel it describes — annotate as superseded rather than leaving the recipe as the last word.
