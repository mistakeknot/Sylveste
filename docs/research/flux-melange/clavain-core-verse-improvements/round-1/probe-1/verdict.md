# f-017 verdict — PARTIAL (downgrade P0 → P2)

The documentary half of f-017 confirms cleanly: four Sylveste docs (handoffs 2026-04-21:59, 2026-04-18 ×2, 2026-05-14:37, research 2026-04-28:65) plus closed bead sylveste-2uhz and live doc `interpub/.claude-plugin/commands/release.md:53` prescribe raw `sqlite3 DELETE FROM publish_state` or a fictitious "re-run to force", and none was ever migrated to `ic publish unlock`. The currency half refutes: `ic publish unlock` only shipped 2026-06-19/22 (commits 5170fa0, c4e9b2a — closing sylveste-2uhz), two months *after* every cited handoff, and the same commits made the kernel self-healing — failure paths now delete their own lock row (engine.go:217-224) and the next plain `ic publish` auto-clears any stale row (error recorded, or >1h untouched; engine.go:167-190, publish.go:121-126), with the error text itself now naming `ic publish unlock`. No bead or doc shows a hand DELETE executed after June 2026, so "still the de-facto production recovery" overstates a problem that has shrunk to unmigrated historical docs plus one live wrong line in interpub's release.md. Coverage check: `unlock` covers every stuck state the raw DELETE covers (same `phase NOT IN (done,idle)` predicate, state.go:163-179), but it carries the same hazard it replaces — no IsStale guard, no --force, no --dry-run, so it can delete a genuinely in-flight lock and let two publishers race version bumps and marketplace.json edits (the blast radius of the hand DELETE is workflow-level double-publish, not DB corruption; SQLite WAL makes the SQL itself safe). It also lacks a by-name form (`ic publish unlock <plugin>`) and only opens the cwd-resolved DB, while stuck rows are documented to live in either the monorepo or plugin-local `.clavain/intercore.db` (handoff 2026-04-21:60). The highest-leverage fix is a one-line consumer migration: rewrite `interverse/interpub/.claude-plugin/commands/release.md:53` to name `ic publish unlock` (and note the 1h self-clear), then annotate the four historical docs and close/rewrite the stale open bead Sylveste-46v whose premise ("no auto-recovery, build an unstick command") is already shipped. Unknowns: the out-of-repo agent-memory recipe at `~/.claude/projects/-Users-sma-projects-solwend/memory/reference_intercore_publish_stuck_locks.md` (cited by the 2026-05-14 handoff) was not read and will keep resurfacing the bypass until edited; the live `.clavain/intercore.db` was not inspected for pre-fix stale rows still blocking plugins; whether zklw-side docs diverge was not checked.

REMEDIATION: Amend `interverse/interpub/.claude-plugin/commands/release.md:53` to replace "Re-run to force, or check `ic publish status`" with `ic publish unlock` guidance, annotate the four handoff/research docs as superseded by the June 2026 self-healing + unlock, and close open bead Sylveste-46v as already-shipped.
