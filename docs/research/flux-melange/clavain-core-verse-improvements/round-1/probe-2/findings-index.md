# probe-2 findings index — fd-kernel-contract (lock + event internals)

CONFIRMED f-038 (P0): `internal/lock/lock.go:329-351` `tryBreakStale` breaks any lock older than 5s purely by `owner.json` created timestamp — no PID-liveness check, no heartbeat — while `AGENTS.md:80` and `docs/product/intercore-vision.md:377` claim PID-liveness stale detection.
CONFIRMED f-039/f-040/f-041 (P0/P1): 15 of 16 `dispatch.New` call sites in `cmd/ic` wire a nil event recorder; the one wired path fires the event post-commit, fire-and-forget, contradicting the "same transaction" doc comment at `internal/dispatch/dispatch.go:237`.
CONFIRMED f-018 (P1): `cmd/ic/events.go:136-160` advances the consumer cursor on stdout write with no ack/replay path; 24h cursor TTL (events.go:268, 749-753) appears in no contract doc.

P0 | fd-kernel-contract | core/intercore/internal/lock/lock.go:329-351 | tryBreakStale breaks locks on timestamp alone (StaleAge default 5s, lock.go:17); `pidAlive` exists (lock_unix.go:11) but is only consulted by `Clean` (lock.go:270), never on the acquire/break path — mutual exclusion void for any holder >5s [t]
P1 | fd-kernel-contract | core/intercore/AGENTS.md:80 + docs/product/intercore-vision.md:377 | docs claim "PID-liveness stale detection" / "Stale detection checks PID liveness (`kill(pid, 0)`)" — false for the break path that actually matters (acquire); only the manual `ic lock clean` subcommand checks liveness [t]
P1 | fd-kernel-contract | core/intercore/lib-intercore.sh:335-353 | bash fallback lock path never breaks stale locks (pure mkdir spin, 10 retries) while the Go path breaks at 5s — same helper, two divergent semantics depending on whether `ic` is on PATH [t]
P1 | fd-kernel-contract | os/Clavain/hooks/lib-sprint.sh:849 | sole production lock holder (`sprint-claim`, bought to prevent a TOCTOU double-claim per comment at lib-sprint.sh:835) is exactly the lock a false break defeats: two sessions both pass the active-agents check → dual sprint ownership [t]
P0 | fd-kernel-contract | core/intercore/cmd/ic/dispatch.go:145,176,207,251,298,334,369,429,491 | all 9 CLI dispatch paths (spawn/status/list/poll/wait/kill/prune/tokens/retry) construct `dispatch.New(d.SqlDB(), nil)` — status transitions from poll/wait/kill/retry leave zero rows in dispatch_events [t]
P0 | fd-kernel-contract | core/intercore/cmd/ic/gate.go:66, situation.go:51, cost.go:131,207, run_config.go:372,425, run_lifecycle.go:584 | 6 more nil-recorder construction sites — gate evaluation, cost reconcile, and run-config dispatch mutations are also invisible to the event stream [t]
P1 | fd-kernel-contract | core/intercore/internal/dispatch/dispatch.go:237 vs 295-306 | doc comment "Records a dispatch event in the same transaction when an event recorder is set" is false: recorder fires after `tx.Commit()`, fire-and-forget, and the sole wired recorder swallows insert errors at slog.Debug (run_lifecycle.go:132-134) — a crash between commit and recorder loses the transition permanently [t]
P1 | fd-kernel-contract | core/intercore/cmd/ic/run_lifecycle.go:137 | `run advance` is the ONLY path that records dispatch events; anything spawned via `ic dispatch spawn` and polled via `ic dispatch poll/wait` (the documented dispatch flow) produces no durable transition record [t]
P1 | fd-kernel-contract | core/intercore/cmd/ic/events.go:136-160 | cursor saved per batch after successful stdout encode; downstream consumer crash (interverse/interspect/hooks/lib-interspect.sh:2541-2575 between `ic events tail` and `_interspect_insert_evidence`) silently drops the whole batch — at-most-once delivery, no ack, no replay [t]
P2 | fd-kernel-contract | core/intercore/cmd/ic/events.go:268,749-753 | non-durable cursors carry a 24h TTL (only `--durable` register opts out); neither the TTL nor the delivery semantics appear in contracts/events/README.md or COMPATIBILITY.md — a consumer idle >24h silently restarts from 0 or its saved position depending on registration history [t]
P2 | fd-kernel-contract | core/intercore/docs/product/intercore-vision.md:345 | vision promises "no event is pruned while any durable consumer's cursor still points before it" — no `ic events prune` subcommand exists (events.go subcommands: tail/cursor/emit/record/list-review/list-agency), so the guarantee is unenforceable either way [t]

## Test pins / gaps
- `internal/lock/lock_test.go:90 TestStaleBreaking` PINS the bad behavior: backdates owner.json, asserts break with no liveness check. No test covers PID-alive-should-not-break on the acquire path.
- `internal/dispatch/dispatch_test.go` — zero references to eventRecorder/AddDispatchEvent; nil-recorder silence is untested in both directions.
- `cmd/ic/events_test.go` — only tests `events record` idempotency; cursor save/advance behavior in `cmdEventsTail` has no test.
- `go test ./internal/lock/ ./internal/dispatch/ ./internal/event/` — all pass (2026-08-06); the pins above are green.
