# probe-3 findings — fd-kernel-contract (gate integrity cluster)

CONFIRMED | fd-kernel-contract | f-043 | The default ship gate passes on self-report alone; the only executed-verification gate is opt-in by bead label. Default kernel gates (core/intercore/internal/phase/gate.go:118-145) check only existence of agent-registered artifacts (`ic run artifact add`) and agent-written verdict sidecars (internal/dispatch/collect.go:80-84,189-216); the ship skill itself requires no verification at all (os/Clavain/skills/ship/SKILL.md:8 "Use when verification is already done"); the sole executed-proof gate (`runtime_evidence` via runtimeproof.VerifyFile, gate.go:490-547) fires only for runs/beads opted in via the `close-gate:runtime-evidence` label or `--runtime-evidence` flag — exactly 1 bead carries that label across all ~/projects trackers — and hooks.json has no PreToolUse Bash matcher, so `git push`/`bd close` bypass the entire gate stack structurally.

PARTIAL | fd-kernel-contract | f-048 | Self-weakening of P1 gates is CONFIRMED but the mechanism attribution is wrong: `commands/degraded-modes.yaml` is advisory-only — no code reads it; sprint.md:579 instructs the agent to consult it, so editing it weakens only the agent's own behavior. The code-enforced lever is project-level `.clavain/agency-spec.yaml` (resolved at os/Clavain/cmd/clavain-cli/compose.go:423-430): `gate_mode: off` makes hooks/lib-sprint.sh:1090 return 0 with NO log line, `gate_mode: shadow` makes lib-sprint.sh:1141-1144 always pass, and the Go enforce-gate defaults to shadow on any spec-load error (cmd/clavain-cli/phase.go:355-361,378-384; handoff.go:693-702). The "admin-controlled config, same trust level as Makefiles" claim (lib-sprint.sh:1018-1020) is false — the file lives in the agent-writable working tree and no audit event is emitted on mode read or on `off`.

P1 | fd-kernel-contract | os/Clavain/cmd/clavain-cli/phase.go:394-404 vs os/Clavain/hooks/lib-sprint.sh:1107-1124 | f-042 CONFIRMED and still live: the Go enforce-gate (invoked by commands/sprint.md:348,413) fails open — no ic run → "skipped" return nil, ic unavailable → return nil — while its bash twin fails closed for design/ship stages since sylveste-scx1; an absent kernel silently waves the ship gate in the Go path.

P1 | fd-kernel-contract | core/intercore/internal/budget/budget.go:121-126 + cmd/ic/dispatch.go:443, gate.go:74, run_config.go:428, run_lifecycle.go:218 + internal/phase/gate.go:472-477 | Budget enforcement is doubly inert: `budget.warning`/`budget.exceeded` events are dead (recorder nil at all four cmd call sites; emitEvent no-ops), and the gate check itself passes when the querier is nil ("no budget querier provided, skipping" → GatePass) — overspend neither blocks nor leaves a durable event.

P1 | fd-kernel-contract | core/intercore/lib-intercore.sh vs os/Clavain/hooks/lib-intercore.sh (both stamped INTERCORE_WRAPPER_VERSION="1.1.0", 214 diff lines) | f-001/f-025 CONFIRMED: the two wrapper copies diverge in failure direction under an identical version stamp — `intercore_sentinel_check_or_legacy` throttles via legacy temp-file fallback in the core copy but fails open ("no ic = allow") in the Clavain copy; `intercore_state_set`/`state_get`/`sentinel_check` also disagree on unavailable-ic return codes; `intercore_gate_check`/`gate_override` fail open in both.

P2 | fd-kernel-contract | os/Clavain/config/agency-spec.yaml:16 (`defaults.gate_mode: shadow`) + os/Clavain/hooks/lib-sprint.sh:1089 + os/Clavain/cmd/clavain-cli/phase.go:360,383 | The shipped fleet default for spec gates is shadow (warn-only), and the missing-spec defaults disagree: bash falls back to "enforce" when the spec is absent while Go falls back to "shadow" — the same absent file yields opposite enforcement postures depending on which binary the workflow invokes.

P2 | fd-kernel-contract | core/intercore/internal/phase/gate.go:157-174 + internal/phase/machine.go:49,189-221 + cmd/ic/run_lifecycle.go:79 | Soft-tier gates warn-and-advance on `ic run advance` (only hard blocks), while the `ic gate check` dry-run exits 1 on any fail including soft (cmd/ic/gate.go:216-219) — bash consumers of the dry-run therefore block on gates the kernel itself would wave through; priority ≥ 4 / --disable-gates bypass is logged only via slog.Warn to stderr, not as a typed audit event.

P2 | fd-kernel-contract | os/Clavain/hooks/hooks.json (PreToolUse matcher covers only Edit|Write|MultiEdit → guard-plugin-cache.sh) | Gate stack is structurally advisory: nothing intercepts Bash, so an agent can run raw `git push` and `bd close` and never touch bead-close.sh, enforce-gate, or ic gate check; every fail-closed gate is opt-in at the call-site level.

P3 | fd-kernel-contract | core/intercore/contracts/cli/gate-*.json + COMPATIBILITY.md | No documented failure-direction policy exists anywhere in contracts/ or docs — fail-open vs fail-closed is per-call-site folklore (inline comments only), so plugin authors cannot code against a stated contract for kernel-absent behavior.
