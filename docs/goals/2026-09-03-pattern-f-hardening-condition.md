OUTCOME: the offload shape becomes self-correcting instead of hand-journaled. Verified by: interstat released so the estate runs the fixed parser; every Pattern F executor and validator verdict lands in the interspect evidence register with a verdict kind that distinguishes a replay of the verify block from an independent finding, and a failed write is loud; plan-gauge-lint.py runs as a precondition before any plan is handed to an executor, and a plan that fails it never spawns one; the validator contract carries the second channel ("what the gauge did not check") as a named output; one real goal runs as a fresh session per pilot and its execution-only profile (review lanes excluded) is compared against goal 1b53da77's 65% cost share / 344K per turn. Stop after 16 turns.

GATE: which gate metric stands is mk's ruling, not the goal's -- until ruled, report BOTH generated-token share and cost share, execution lanes only. Pass rate of executor runs stays at or above 5/5 verbatim application; a gauge defect counts against the plan author, not the executor. No hand-typed verdict tables: if a verdict is not in the register, it did not happen.

DONE WHEN: interstat released, verdicts visible in interspect for every run in the window, gauge-lint wired as a precondition with one demonstrated refusal, validator contract updated, the fresh-session trial profiled and journaled, and Sylveste-yibw's children reflect what shipped.

OUT: the local-model cascade, effort tuning, re-deriving the phase table, the tripwire beads, and Sylveste-koeo's round-trip fix unless it blocks the release.
