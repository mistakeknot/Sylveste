# Journal — Clavain replay channel

Session aa2bb078, 2026-09-06. Orchestrator: Fable 5.1 main thread. Executor for the brief: role-resolved `routine-execution` (codex gpt-5.6-sol, via `scripts/dispatch.sh --role`). Validator: role-resolved `validation` (claude-fable-5-1). Register: `~/projects/Sylveste/.clavain/interspect/interspect.db`, `--db` explicit on every write.

## Timeline

- Probe (Haiku, scratch worktree): under `--permission-mode dontAsk --allowedTools Bash` the seat runs commands; `--add-dir` makes an outside file readable; a sandbox `denyWrite` on the checkout blocks `touch` in it but also blocks `~/.cache/uv` and the venv sync, so `uv run pytest` fails before pytest starts. Decision: no sandbox denial; git-snapshot mutation guard.
- Frozen acceptance for `--apply`: `tests/structural/test_plan_gauge_apply.py`, 12 tests, 10 red and 2 green by construction before implementation.
- Goal minted 5bdf10a5 (bound to Sylveste-soj7, complexity 4). Freeze commit 612f8f0 (the 12 apply tests).
- Brief `brief-gauge-apply.md` (Contract: brief) gauge-linted clean; executor dispatched by hand through `scripts/dispatch.sh --role routine-execution` (codex gpt-5.6-sol) at 22:12; packet at 22:5x: commit c03442c, 30 passed, SELF-TEST PASSED, gate bats 8, frozen-intact, commit scope one file. Orchestrator re-ran the same checks on main: identical. Register: executor replay PASS (c03442c).
- Seat fix built on worktree branch `goal-replay-seat` while the executor held the main checkout: df848dc, 8 bats green (`tests/shell/dispatch_claude_seat.bats`), no regression in the other dispatch suites, the real-Intercore role integration test PASS. Cherry-picked onto main as 817473c.
- First launch of the two seat demonstrations failed before any model ran: `cannot persist started routing decision` (terminal_recording) because the scratch checkout had no Intercore store; `ic init` there fixed it. Not a seat defect, but a footgun for any dispatch into a fresh worktree.
- Deliberately broken checkout: scratch worktree at c6bc883 (`--list` prints the goal column where the commit column was; test 1 of `pattern_f_verdict.bats` fails only when run). Two validators dispatched through the fixed seat with a receipt nonce written after the prompts were fixed: demo-broken (must FAIL naming the test) and demo-unrun (Verification calls `bats-replay-runner`, a program that does not exist; must UNRUN).
- Exact plan `exact-verdict-unrun.md` (UNRUN verdict value, new bats file, seven doc edits) gauge-linted clean after path-qualifying its edit markers (the linter attributes a fence to the nearest path in the six lines above it, which for a doc edit that names `scripts/dispatch.sh` is the wrong file; `old_string (<path>):` beats it). Applied by the tool: `plan-gauge-lint.py --apply` → 15 steps ok (1 precondition, 11 edits, 1 create, 2 verify fences), committed 59ceddf by the orchestrator per the plan's Commit section. No executor model.
- demo-broken returned `VERDICT: FAIL`, criterion naming test 1 with its bats line, "10 ok, 1 not ok", `RECEIPT: receipt-e7b7462cde` (matches the nonce). Its second channel found a defect in the demo brief's own gauge: `bats ... | tail -4` masks bats' exit status, so the frozen Verification is green on this red test; filed as a gauge bead (GAUGE006 successor). The seat's report also carried an "Insight" block inherited from the operator's output style; filed P3.
- demo-unrun returned `VERDICT: UNRUN`, `RECEIPT: receipt-e7b7462cde` (matched): the seat refused to rule when `bats-replay-runner` did not exist instead of guessing from the packet. Register: replay UNRUN row written through the updated script and read back.
- Exact-plan validator (Fable seat, producer = the tool): `VERDICT: PASS`, `RECEIPT: receipt-a5cf3e29a1` (matched). Register: validator replay PASS at 59ceddf.
- Exact-plan validator second channel, five findings registered: the prompt template still says "run its Verification" and does not cover an exact plan's Preconditions (fixed in the doc on the main thread before publish); the producer identity for a tool-applied plan is undefined (doc sentence added: the author model; same-model author and seat is context separation only); the RECEIPT protocol has no tool enforcement (to Sylveste-uo7b's successor scope); CRITERION wording for UNRUN disagrees between template and outputs (fixed); the new bats file's setup skips read as ok through the tail pipe (uo7b).
- Brief validator (Fable seat, producer codex gpt-5.6-sol): `VERDICT: PASS` at c03442c, `RECEIPT: receipt-460d49d9e0` (matched). Second channel, five findings registered: a fence timeout counted as a match under two expectation forms (reproduced), a Verify fence skipped silently when the old heuristic does not classify it (reproduced), the revert hint fails once a Create landed (a flaw in the brief itself), fences on stdin and the timeout not killing the process group, encoding round-trip. The first three are fixed on the main thread with red-first tests before publish (a task under thirty minutes; the register carries the findings that drove it); the last two are a P3 bead.
- Follow-up on the main thread (a task under thirty minutes; the register rows above are its provenance): 167a369 fixes the contracts doc (exact-plan prompt, UNRUN criterion, producer identity for a tool-applied plan); 328f0cf fixes the three reproduced `--apply` defects, each red first in `tests/structural/test_plan_gauge_apply_followup.py` (timeout never matches; the Verify heading is authoritative; the revert hint separates `git checkout --` from `rm -f`). Gauge suites: 33 passed, SELF-TEST PASSED, gate bats 8 ok. Full structural suite on main: 964 passed, 1 skipped.
- Register at close: 19 rows for this goal under session aa2bb078 (2 executor replays, one of them the tool; 4 validator replays: FAIL, UNRUN, PASS, PASS, every receipt matched; 13 independent findings), all read back with `--list`.
- Full bats on the main checkout: 818 tests, 2 not ok in `test_runtime_evidence_canary.bats`. Bisected in fresh worktrees at 612f8f0, c03442c, 817473c, 59ceddf and 328f0cf: 0 not ok at every one; the two failures are main-checkout state (untracked agent files and the monorepo intercore status the test inspects), not code. CI Plugin Tests green on 328f0cf.
- Publish attempt 1 (zklw, `ic publish 0.6.309`): refused in release preparation. The CLI's windows/amd64 release build fails since 45a9d47 (2026-09-05, "govern accepted review execution through Intercore"): `review.go` and `review_worker.go` use flock, Setsid, Setpgid, Getpgid and Kill without build tags. Outside this goal's scope but blocking its DONE WHEN; fixed on the main thread by moving those calls into `review_process_unix.go` / `review_process_windows.go` (the split `runtime_process_*.go` and `calibration_lock_*.go` already use), behaviour-identical on unix.
- Publish attempt 2 after f04bb56: `ic publish 0.6.309` from zklw succeeded (release canary registered, post-release probe passed); Mac cache refreshed, `diff -rq` cache vs source: 0 lines; dispatch.sh, plan-gauge-lint.py, pattern-f-verdict.sh, the contracts doc and the seat bats byte-identical. Clavain main at 1b847ab (bump). Sessions running 0.6.308 hooks keep the old dispatch.sh until restarted; direct `bash scripts/dispatch.sh` calls from the checkout already use the new one.
- Closed: Sylveste-soj7 (against 817473c and 59ceddf); ic goal 5bdf10a5 closed with successor ref Sylveste-uo7b. Residual beads: Sylveste-uo7b (P1, pipe-masked gauges; RECEIPT enforcement), Sylveste-n8jf (P3, seat output style), Sylveste-ihnv (P3, --apply stdin/timeout/encoding).
- Turn budget: 10 stated; about 28 tool-bearing turns. The overrun came from four things the goal text did not price: the executor holding the main checkout (seat built in a worktree, then cherry-picked), the Intercore-store footgun on the scratch checkout, thirteen second-channel findings that were worth fixing before publish, and a windows build break outside the goal that blocked the publish.
