# Journal — interstat one meter (goal 7be37994)

Session aa2bb078, 2026-09-06. Orchestrator: Fable 5.1 main thread. Executor: role-resolved `routine-execution` (codex gpt-5.6-sol, reasoning high, via `scripts/dispatch.sh --role`). Validator: role-resolved `validation` (claude-fable-5-1, a different model from the producer). Contract: `brief` (the acceptance is a frozen test file, so the executor owns the mechanics). Register: `~/projects/Sylveste/.clavain/interspect/interspect.db`, `--db` explicit on every write.

## Why this goal, and what changed under the draft

The Astra-reviewed draft (2026-09-04) assumed the estate path would consume `claude_attribution.parse_claude`. Between the review and the mint, the sibling Codex session 01a0742a landed that parser (interstat ede848c) as a manifest-only, strict instrument. Measured before minting: `parse_claude` marks every request without `apiBlockIndex` invalid; a survey of all 4,062 transcripts under `~/.claude/projects` found the field in 0 of ~271,000 assistant messages before September 2026 and in 41,886 of 52,864 in September; on a 59 MB transcript from 2026-09-04 it returned 0 requests and 7,035 `invalid_usage` issues. The explicit 1h/5m cache-write split, by contrast, is present in 100% of assistant messages in every month. So the goal was amended at mint (see the condition file): one shared lenient extractor for the estate path, one TTL pricing rule in `cost.py`, and an agreement test against the strict parser on a transcript both accept.

## Preflight (before any edit)

- Suite green at ede848c: 177 passed (`uv run --with pytest --with pyyaml pytest -q`; the repo has no test workflow of its own, only secret-scan).
- Stray untracked `.beads/` store at the interstat root (embedded dolt, 1.3 MB, 0 issues, born 2026-09-04 or 05) removed; `uv.lock` drift (0.2.26 → 0.3.5) committed as 1c9f812.
- Ownership: Sylveste-balk and Sylveste-4yb3 claimed and labeled `ic_goal_id:7be37994`; note on Sylveste-yibw naming this goal and its files. intermute was down (port 7338 refused), so no interlock reservations could be consulted; the measured-delivery lane that holds yibw showed "Goal hit usage limits" in intermux with a clean interstat tree.
- Goal minted 7be37994 (second attempt; the first condition file was 5,246 chars against the 4,000 cap, so the amendment moved to the charter).

## Acceptance frozen (interstat 2d04ca0)

`tests/fixtures/one-meter/` (generated by `make_fixtures.py`, checked in) and `tests/test_one_meter.py`, ten tests: extractor dedupe and TTL read; `calc_cost` 1h at 2x; agreement with `parse_claude`/`request_cost` on the modern fixture (the strict parser accepts it: 2 requests, 0 issues, $0.04487 + $0.00708); `agent_runs` keyed by `source_path` with the per-(transcript, model, day) breakdown and idempotent re-ingest; hook rows claimed oldest-first by subagent type; `--backfill` receipt; `cost.run_report` by model/lane/day from the breakdown with legacy rows still counted; `cost-query.sh by-phase-model` and `cost-usd` from the breakdown; `profile.py` through the shared extractor; `pricing_lower_bound` in the summary. Red at the freeze commit for the right reason: `ModuleNotFoundError: No module named 'claude_usage'`. The 177 existing tests stayed green.

## Executor

Brief at `scratchpad/brief-one-meter.md` (gauge lint: `contract=brief; no gauge defects found`, rc 0). Dispatched 2026-09-06 20:5x PDT: `dispatch.sh --role routine-execution -C interstat --prompt-file prompt-one-meter-exec.md`, resolved to `codex exec -m gpt-5.6-sol -c model_reasoning_effort=high`, sandbox workspace-write, pid 59450. The three marker lines open the prompt; the spawn gate is a Claude PreToolUse hook and does not fire for a Codex dispatch, so the lint was run by hand before the dispatch. Compile check for Python is inside the brief's Verification (a brief assembles nothing before spawn, so there is nothing to compile pre-spawn; recorded as the GATE's compile step).

Executor packet (strike 1, 10 minutes): commit fb398b6, seven files (`claude_usage.py` new, `analyze.py`, `profile.py`, `cost.py`, `cost-query.sh`, `init-db.sh`, a schema note in `CLAUDE.md`), all eight Verification commands green (10 passed, 187 passed, frozen paths untouched, dry run prints 3), no unresolved questions. The orchestrating session re-ran the block on the checkout before dispatching the validator: identical. Register: executor replay PASS written and read back.

Validator: dispatched through `dispatch.sh --role validation --producer-identity gpt-5.6-sol`, resolved to `claude --model claude-fable-5-1 --permission-mode dontAsk --disallowedTools Edit,Write,NotebookEdit -p` (the review seat). Verdict after 9 minutes: `VERDICT: PASS`, `CRITERION: none`, eight BEYOND THE GAUGE bullets. The first bullet is a confound that outranks the verdict: the seat could not execute anything (every pytest, uv, python3, and bash -n call was denied by `--permission-mode dontAsk`, and the brief outside `-C` was unreadable), so the replay was a hand trace of the ten tests against the fixtures plus git-verified facts. The executed replay of record is the orchestrating session's own run of the Verification block on the checkout before dispatch (187 passed, frozen paths untouched, dry run 3). Filed as Sylveste-soj7. Register: validator replay PASS (note carries the confound) and eight independent FAIL rows.

Rule-4 follow-up (main thread, 3a315df): five of the eight findings were gate-relevant and small, so they were fixed here with tests written first (`tests/test_one_meter_followup.py`, 5 failed before, 192 passed after): `session-cost` with an auto-detected session id priced the whole database because the report source was built once at startup (now rebuilt after the filter is set; the id file is overridable through `INTERSTAT_SESSION_ID_FILE`); hook rows are claimed in the order runs ended, not transcript path order; a subagent without `.meta.json` still claims the session's oldest unparsed hook row and keeps its semantic name; any pricing unknown labels a report a lower bound; `connect_db` no longer downgrades a newer schema version. The other three (window semantics in mixed databases, silent malformed-line drops, the unrounded JSON total) are in Sylveste-y8xx. Register: executor replay PASS under this session's id with the rule-4 note.

## Historical manifest (before numbers)

Windows fixed in `interstat/docs/research/2026-09-06-one-meter-historical-manifest.md`; `before` = the installed 0.3.5 `profile.py` over the same windows (JSON kept in the scratchpad `manifest/before/`). `after` = fb398b6's `profile.py` over the same windows. Whole-window cost rises in every main-heavy window, 10 to 24 percent, because the main lane's cache writes are 1h writes billed at 2x input and the old profiler priced them at the 5m 1.25x; executor and validator lanes, which write at 5m, do not move. One window fell (c4cda02c orchestrator, -3.5%) because the old profiler counted API-error lines that carry usage but were never billed. Totals over the thirteen windows: $146.61 → $164.09 (+11.9%). No window is a lower bound: every request carried the explicit split. The three measured sections in `commands/model-routing.md` now carry re-measurement blocks with the old tables labelled superseded; one conclusion changed (pilot G's orchestrator was 53% of its run, not 47%, so no Pattern F run has yet put the main thread under half of cost).

## Publish, refresh, backfill

interstat 0.3.6 published from zklw (`ic publish 0.3.6 --cwd=…`, dry run then real: marketplace synced, canary registered, post-release probe passed, bump 38b87ef pushed by ic). This Mac refreshed 0.3.5 → 0.3.6; the seven shipped files (`claude_usage.py`, `analyze.py`, `profile.py`, `cost.py`, `cost-query.sh`, `init-db.sh`, `hooks/session-end.sh`) are byte-identical between the cache and interstat main. The 0.3.5 cache directory was marked orphaned at 21:24 and retained for live sessions.

Live database backed up to `~/.claude/interstat/metrics.db.bak-20260906` (151 MB), then `analyze.py --backfill` from the shipped 0.3.6 cache, 1 minute 41 seconds:

```
{"rows_before": 11962, "rows_after": 13883, "transcripts_seen": 4052, "transcripts_stored": 3850, "rows_without_source_path": 10033, "rows_ttl_unreported": 2}
```

Rows rose from 11,962 to 13,883 because 1,921 transcripts had never reached a row (the old upsert collapsed same-named subagent files); 202 of 4,052 transcripts carry no assistant usage and were skipped; 10,033 rows keep no `source_path` (hook rows whose transcripts are gone, or pruned corpora); 2 rows lack the TTL split. `agent_run_usage` holds 4,569 rows over 3,850 runs; `PRAGMA user_version` is 7. All-time API-equivalent total moved from $49,451 to $50,135 (+1.4%): most history is legacy rows without transcripts, so the estate total barely moves, but the by-model table did, which was Sylveste-balk's point: opus-5 433 → 844 runs and $11,642 → $9,769, fable-5 66 → 215 runs and $10,106 → $11,170, fable-5-1 68 → 87 runs and $484 → $1,651, sonnet-5 590 → 1,762 runs and $2,469 → $2,004. The report is labelled a lower bound (2 rows).

SessionEnd reconciliation through the shipped hook: `hooks/session-end.sh` fired by hand for finished session 3807a93b (pilot L) ran the 0.3.6 parser in about two seconds; the three rows it wrote (main fable-5-1, subagent opus-5, subagent sonnet-5) match `profile.py --session 3807a93b` token for token, cost $9.9883 against $9.99, one breakdown row each.

Old writers: 16 Claude Code processes were running at 21:27, all started before the refresh (latest 20:24), so their SessionEnd hooks still run the retained 0.3.5 parser; their rows carry no breakdown until the next `analyze.py --backfill`, which re-ingests them idempotently. They are the sessions in intermux's roster at that time; none is listed by id here.

## Register (read back, never transcribed)

`select role, verdict, count(*) … where plan like '%brief-one-meter%'`: executor PASS 2 (fb398b6, 3a315df), validator PASS 1 (replay, hand trace noted), validator FAIL 8 (independent). Eleven rows, every write rc 0 and read back with the nonce check. No gate row: the Codex dispatch does not pass through the Claude PreToolUse gate, so the lint ran by hand (rc 0) before the spawn.

## Close-out (2026-09-06)

DONE WHEN met: Sylveste-balk and Sylveste-4yb3 closed against fb398b6 and 3a315df on interstat main; fixtures (`tests/fixtures/one-meter/`, `make_fixtures.py`) and the historical manifest (`docs/research/2026-09-06-one-meter-historical-manifest.md`, 6f59c54) in the interstat repo with before, after, and delta for thirteen windows across the four goals, JSON in this goal's evidence directory; the three measured sections in Clavain `commands/model-routing.md` carry re-measurement blocks with the old tables labelled superseded and one changed conclusion stated (f48fd65 on Clavain main; not republished, because Clavain main also carries the measured-delivery lane's unreleased work, so the cached command file stays at 0.6.308 until the next publish); interstat published, refreshed, backfilled with the receipt above; old-writer sessions counted and their re-ingest path named.

GATE held: balk and 4yb3 claimed and yibw noted before the first edit; `claude_attribution.py`, `task_attribution.py`, `codex_history.py`, and `tests/` untouched by the executor (`git diff --stat 2d04ca0` empty); the compile step lived in the brief's Verification (a brief assembles nothing before spawn), and the orchestrator ran it; executor and validator resolved through the role table (routine-execution → codex gpt-5.6-sol high, validation → claude-fable-5-1), producer and validator on different models, no fixed-model override; every attempt, verdict, and finding in the register with `--db` explicit; absolutes throughout, no share-based claim, no predetermined pass count. Executor 1 spawn, 1 commit, strike 1; validator strike 1 on criteria; code-correct 1/1 by the executed replay; five second-channel defects fixed on the main thread under rule 4.

Turn budget: 12 stated; about 22 tool-bearing turns on this goal, with a prompt audit mk requested interleaved (about 6 turns of its own). The overrun is real and mostly the shape's cost: mint, freeze, dispatch, wait, validate, fix, publish, refresh, backfill, reconcile, re-measure, correct docs, close.

Interpretations held as recorded at mint, with the amendment: the estate extractor is lenient because the strict parser rejects every request before 2026-09-05; agreement is pinned by test where both apply.

Confounds: the first mint attempt failed the 4,000-character condition cap (the amendment moved to the charter); the validation seat cannot execute (Sylveste-soj7), so this goal's replay rests on the orchestrator's run; the executor's Monitor printed an empty process count because the Codex child is not a `claude -p` process (cosmetic).

Successor: Sylveste-soj7 plus a linter `--apply` mode, so that both halves of the replay channel become real (recommended in the closing message with the lineage check).
