OUTCOME: One goal's execution is driven by scripts/orchestrate.py rather than by hand. Verified by: the orchestrator resolves executors and validators through dispatch.sh --role, gives each executor a fresh worktree with ic init run in it and merges its commit back, runs an exact plan through plan-gauge-lint.py --apply instead of a model, dispatches the validation seat with --plan and a receipt nonce it writes itself and records UNRUN when the RECEIPT line does not match, writes every verdict through pattern-f-verdict.sh with --db explicit and reads it back, runs dispatch in its own process group and kills the group on timeout so a grandchild can never outlive its deadline (mk-kj2m), and prints one closing packet; interstat reports the main-thread share of the run's cost and the orchestrator's turn count; demonstrated on one live goal-sized item from the backlog with a brief and an exact plan. Stop after 10 turns.
