# Goal c4cda02c — journal (Pattern F live)

Successor to goal c60de386. Orchestrating session aa2bb078 (Fable 5.1) mints, briefs, publishes, and closes; each code pilot is orchestrated by its own fresh headless session (`claude -p --model fable --session-id <id>`) that receives the contract block plus a brief, writes the plan, lints it, spawns a Sonnet executor and an Opus validator, and records verdicts with the register script. The live-session pilot (G) is different on purpose: it starts after the Clavain publish, is told not to lint, and invokes `/execute-plan` so the session's own PreToolUse gate does the gauging.

## Pilots

| Pilot | Work | Session | Executor commit | Validator | Gauge defects (plan author's) | Beyond the gauge | Register rows |
|---|---|---|---|---|---|---|---|
| E | gate refusals write a register row (role gate, kind gate); verdict notes survive the sanitizer (base64 with `note_enc` on rejection, exit 5 when even that is rejected); `--list` via `sqlite3 -json` + jq (Sylveste-yibw.12, .13 sanitizer half) | b96b269d | 25077ad (strike 1) | PASS (strike 1); all 11 VERIFY steps re-run (verdict bats 9, gate bats 7, structural 901) | none; the orchestrator added two things beyond the brief (the gate bats pins `CLAUDE_PROJECT_DIR` to a tmp path and unsets `INTERSPECT_DB` so a recording refusal can never hit a live register; the hook's note drops the `pattern-f gauge gate:` prefix and shortens the plan path) | 9 bullets, 5 registered: the encode branch re-shrinks only the note, so a long criterion plus an encoded note can still orphan a row; the gate note keeps `plan-gauge-lint refused <basename>:` boilerplate and truncates a second GAUGE finding at 100 chars; `--list` uses jq `@tsv`, which doubles backslashes; exit 5 is reachable through un-encoded fields (a plan basename containing "disregard"); the gate bats uses `run --separate-stderr` without `bats_require_minimum_version`. Three discounted as artifacts of the duplicate instance (see Confounds). The first four go to pilot H | 7 (all rc 0) |
| F | `plan-gauge-lint.py` parses `Create \`path\` with:` blocks into the virtual tree; `.bats` in the path allowlist; fixture PF-C; structural tests (Sylveste-yibw.14) | 9ece15fa | 187ff9d (strike 1) | PASS (strike 1); all 6 VERIFY steps re-run (self-test 7/7, 13 linter tests, structural 901) | none | 7 registered: a `Create` line quoted inside a fence within the two lines above a verify fence classifies that fence as create (same exposure the old/new markers always had); GAUGE005 regressed for created files because `check_stale_artifact` reads only `verify` and `emit` bodies and a bash `Create` block used to classify as `emit`; `CREATE_RE` takes a one-token prose object as a path ("Create fixtures with:") and rejects numbered-list lines and "with the following content:" tails; CHANGELOG still says six fixtures and ten tests; the plan path is not exclusive (duplicate instance); "the pilot driver pushes on each done marker" (wrong: the push at 18:12 was the repo's autosync hook, not the driver). GAUGE005, `CREATE_RE`, and CHANGELOG go to pilot H | 9 (all rc 0) |
| H | fix-forward from E and F: note budget 300 with both shrink loops after encoding and exit 5 when the encoded context still exceeds 480; `--list` joins with a literal tab instead of `@tsv` (backslashes verbatim); the gate note drops the `plan-gauge-lint refused <basename>:` boilerplate; `bats_require_minimum_version 1.5.0`; `CREATE_RE` needs a path-like token and accepts numbered lists and "with the following content:" tails; GAUGE005 scans created files; CHANGELOG | c15f4648 | 6305a89 (strike 1) | PASS (strike 1); all 11 VERIFY lines re-run | none (plan linted clean first pass; 15 edits) | 7 bullets, 6 registered: `CREATE_RE`'s dot-or-slash rule drops extensionless new files (`Create Makefile with:`) and now requires the trailing colon; GAUGE005 double-reports a created `.sh` (body scanned as a block, then again as a file); the note is shrunk before the criterion silently (300 stores 260 with `--commit none`, 140 with a real sha plus a 100-char criterion); the loosened bats assertion covers only the commit-none case; the new exit-5 branch is unreachable because every field is bounded (maximal legal context is 459 chars). All to Sylveste-yibw.15 | 8 (all rc 0) |
| G | live session on clavain 0.6.304: `/execute-plan` on the contracts-doc plan with one seeded gauge defect (Task 2's verify claims `grep -c 'kind gate'` prints nothing while Task 2 writes `--kind gate`) | c0eedb2a | be532a5 (strike 1 after the gate refusal) | PASS (strike 1) | one, seeded by the orchestrating session and journaled here before the run. **The live gate refused the first spawn** with `plan-gauge-lint refused …: GAUGE001 line 54: verify expects no output, but the plan's own emitted text matches`, and the gate itself wrote the first register row (role gate, kind gate, FAIL, commit none, the GAUGE header line as note) before the orchestrator did anything; the orchestrator changed only the `Expected:` line and re-spawned once | 8 registered: the grammar sentence is wrong because the shell heuristic classifies Preconditions and Commit fences as verify (so a Preconditions grep that forbids text an edit writes does fire GAUGE001); `grep -rn TOKEN .` scopes to nothing in `_scope()`, so the Create-block promise over-reaches for that shape; "byte-for-byte" ignores the library's secret redaction, which still runs after the pre-flight; a redacted base64 payload would make `@base64d` fail and abort `--list` mid-stream; `--list` never prints the criterion, so an encoded criterion has no decoder; the refusal write runs before the decision, so a slow register (not a failed one) can time out the hook and let the spawn through; `commands/model-routing.md` line 211 still carries the old grammar claim; plan-not-found refusals carry no GAUGE text. The timeout and the three doc claims are fixed on the main thread under rule 4 (below); the rest to Sylveste-yibw.15 | 10 by the orchestrator + 1 by the gate (all rc 0) |
| main | rule-4 follow-up: the refusal write is bounded by `timeout 15` so the block decision always prints inside the hook's 30s budget; the contracts doc's three over-claims corrected; model-routing.md line 211 corrected and the c4cda02c measurement appended | aa2bb078 | see close-out | none (rule 4, one model end-to-end; VERIFY = gate bats + shellcheck + the real-payload refusal) | n/a | n/a | see close-out |

## Confounds

- Both code pilots were launched twice. The first launch carried the previous goal's id (c60de386) in every `--goal` because the orchestrating session's id extraction matched the echoed condition text instead of the mint output; both were killed within a minute, before any commit, and relaunched with fresh session ids under c4cda02c. The second launch was killed by SIGTERM about thirty seconds in (exit 143; the sandboxed Bash tool reaped the detached processes when its own command ended), after 8 and 9 assistant messages; both were resumed in the same session ids with `claude -p --resume`, outside the sandbox. Each resume re-sends the session's context once more than a clean run would have; the profiles below include it.

## Token profile (execution lanes only)

Filled from `profile.py --session <id>` after each pilot. Comparison points from goal c60de386: orchestrator context 71–82K per turn, orchestrator cost about $3 per pilot, whole pilot about $5; shares for continuity only (cost 63–81%, tokens 80–98%).

| Pilot | lane | msgs | output | cache read | ctx/msg | $ equiv |
|---|---|---|---|---|---|---|
| E (both instances in one session id) | main (Fable 5.1) | 31 | 150K | 3.5M | 121K | 12 |
| E | executor (Sonnet 5) | 19 | 4K | 1.3M | 72K | 0.5 |
| E | validator (Opus 5) | 18 | 4K | 1.2M | 72K | 1 |
| F (both instances) | main (Fable 5.1) | 32 | 110K | 3.0M | 101K | 9 |
| F | executor (Sonnet 5) | 20 | 4K | 0.9M | 53K | 1 |
| F | validator (Opus 5) | 27 | 4K | 1.9M | 73K | 2 |
| H | main (Fable 5.1) | 16 | 60K | 1.4M | 98K | 5 |
| H | executor (Sonnet 5) | 47 | 10K | 3.3M | 73K | 1 |
| H | validator (Opus 5) | 27 | 4K | 1.7M | 67K | 2 |
| G (live) | main (Fable 5.1) | 12 | 10K | 0.6M | 56K | 2 |
| G | executor (Sonnet 5) | 10 | 2K | 0.4M | 49K | 0.5 |
| G | validator (Opus 5) | 27 | 4K | 1.9M | 73K | 2 |

Shares, execution lanes only (cost / generated tokens): E 88% / 99%, F 81% / 98%, H 66% / 85%, G 47% / 85%. Claude Code's own per-instance accounting (`total_cost_usd` in each result JSON): E original $8.54 + duplicate $7.43, F original $9.28 + duplicate $4.08, H $9.04, G $4.30. Absolute reading: the code pilots ran at 98–121K per orchestrator message (longer briefs than c60de386's, two beads each, plus the duplicate instances); the live pilot, whose orchestrator wrote nothing and linted nothing, ran at 56K and $2, whole run $3.

## Verdict register

Read back from the live register, never transcribed: `bash scripts/pattern-f-verdict.sh --list` at close. Grouped read-back (`select session, role, kind, verdict, count(*) … where goal='c4cda02c'`): b96b269d executor replay PASS 1, validator replay PASS 1, validator independent FAIL 5; 9ece15fa 1 / 1 / 7; c15f4648 1 / 1 / 6; c0eedb2a 1 / 1 / 8; plus, under c0eedb2a but with no goal field (the hook has none to pass), the gate row: `gate gate FAIL none | GAUGE001 line 54: verify expects no output, but the plan's own emitted text matches`. The rule-4 follow-up's rows are recorded under aa2bb078 (see Close-out). Every executor, validator, and gate verdict in this goal's window has a row; replays 8 of 8 PASS carried no information, the independent channel carried 26 findings, of which the orchestrating session confirmed four by direct test and fixed them.

## Close-out (2026-09-03, 19:25 PDT)

Shipped on Clavain main: 25077ad (E), 187ff9d (F), 6305a89 (H), be532a5 (G, through the live gate), cacef81 (rule-4 follow-up: the refusal write bounded by `timeout 15`, three contract over-claims corrected, model-routing.md corrected and the measurement appended). Published clavain 0.6.304 (after H) and 0.6.305 (after cacef81) from zklw, canary and post-release probe green both times; the Mac's cache at 0.6.305 is byte-identical to main for the hook, the verdict script, the linter, the contracts doc, model-routing.md, and hooks.json. Running sessions keep executing the version they started on.

Rule-4 follow-up verification, main thread: `bash -n` and shellcheck clean, gate bats 7/7, the real-payload refusal blocks with GAUGE001 and reports `NOT recorded` against a missing register, the allow path prints nothing, contract fences balanced. Recorded as one executor replay PASS row under session aa2bb078 with that note; no validator spawned (rule 4).

DONE WHEN: clavain published and refreshed (twice); Sylveste-yibw.12 and .14 closed with commits; .13's sanitizer half closed in its notes and its quarantine half marked open for mk; the live-session refusal-then-pass journaled with profiles (pilot G); Sylveste-yibw's children reflect what shipped, with .15 holding the residual validator findings. GATE: quarantine untouched (default 48h on every row); absolutes reported per pilot with shares for continuity; executor runs 5 spawns, 4 commits, 1 refusal by the live gate on the seeded defect (the orchestrating session's, as designed), every commit and every validator on strike 1, code-correct 4/4; no hand-typed verdict tables. Turn budget: 12 stated; the orchestrating session used about 40 tool-bearing turns, a third of them on the duplicate-instance incident (see Confounds).

Calibration visibility at close: none of this goal's 44 rows (E 7, F 9, H 8, G 10 plus the gate row, main 1) is visible to interspect's calibration queries until 48 hours after its write, so the earliest is Saturday 2026-09-05 at 18:24 PDT and the last (cacef81's row) Saturday evening; `pattern-f-verdict.sh --list` sees all of them now.

## Quarantine (mk's ruling)

Every row this goal wrote carries interspect's default `quarantine_until` of write time plus 48 hours, so interspect's calibration queries cannot see any of them until Saturday 2026-09-05 at the earliest; `pattern-f-verdict.sh --list` sees them immediately. Whether verdict rows should bypass the quarantine (it is interspect's anti-feedback-loop guard) is open for mk.
