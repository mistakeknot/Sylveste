# Goal condition — interstat one meter (ratified 2026-09-06)

mk approved the plan `quizzical-discovering-dragonfly.md` on 2026-09-06. Goal-shaped rendering by the orchestrating session, not mk's ruling in mk's voice. The parse_claude clause was amended at mint; see the charter.

```
/goal Make interstat's estate meter read what the strict attribution parser reads, key rows by transcript, backfill what it stored, and correct the routing measurements taken with the old meter.

OUTCOME: One usage extraction. analyze.py, profile.py, and cost.py take per-request usage, model, and cache-write TTL from claude_attribution.parse_claude and price through its request_cost, so the estate path and the manifest path cannot disagree on the same transcript; agent_runs keeps one row per source transcript keyed by source_path (never agent_name) with a per-(transcript, model) breakdown, so by-model and by-phase-model are truthful while total_tokens per invocation, run counts, and bead/phase correlation are unchanged (Sylveste-balk); profile.py and cost.py price 1h writes at 2x and 5m at 1.25x from the explicit fields and mark a missing split as a pricing gap rather than pricing it silently (Sylveste-4yb3); a checked-in replay over frozen fixtures (two models and two days in one session, two same-named subagents from distinct files, a streamed response repeated across blocks, explicit 1h/5m splits, a Codex session) fails on the pre-fix code and passes after, and re-ingesting the corpus twice changes nothing; interstat published from zklw, refreshed here with a content diff, stored rows backfilled from their transcripts with a coverage receipt, and one fresh session's SessionEnd ingestion reconciled against a report. Stop after 12 turns.

GATE: balk and 4yb3 claimed and a note on Sylveste-yibw naming this goal and its files before any edit (the measured-delivery lane that holds yibw is stalled on usage limits and intermute is down, so there are no reservations to consult); claude_attribution.py and task_attribution.py are imported, never edited; the stray untracked .beads store at the interstat root and the uv.lock drift are cleared before an executor runs there; acceptance freezes before spawn (fixture expectations, historical manifest, pricing assumptions, verification commands); gauge-lint is necessary and not sufficient for Python, so every plan compiles its assembled post-edit files before spawn; executors resolve through the contracts on disk at mint, producer and validator on different models, any fixed-model trial declared as an override; every attempt, refusal, retry, and verdict in the register with --db explicit and read back; absolutes per attempt, no share-based claim, no predetermined pass count.

DONE WHEN: balk and 4yb3 closed against tested, pushed commits; the fixtures and the historical manifest (full session ids, exact windows, review exclusions, resumed and duplicate instances for goals 1b53da77, c60de386, c4cda02c, ff7fd1a1) in the repo with before/after/delta output; the three measured sections in model-routing.md carry the corrected numbers with the old ones kept and labelled superseded and any conclusion that changed said so; interstat published, refreshed, and backfilled; sessions still writing with the old plugin identified and their rows re-ingested or listed.

OUT: Sylveste-koeo; task_attribution semantics, manifests, and the Astra canary's enrollment (the measured-delivery lane owns them); Pattern F hardening beyond the compile step; calibration and quarantine policy; Sylveste-yibw.15, Sylveste-at90, Sylveste-55bc; effort tuning; share gates; A:L3.
```
