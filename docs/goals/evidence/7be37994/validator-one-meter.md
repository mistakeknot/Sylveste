VERDICT: PASS
CRITERION: none

BEYOND THE GAUGE:
- The replay could not execute anything: every test and script invocation (pytest, uv run, python3 -m, bash -n) was denied by this session's permission mode, and the scratchpad brief itself was unreadable. The verdict rests on a hand trace of all 10 frozen tests in `tests/test_one_meter.py` against the checked-in fixtures and constants, plus git-verified facts: HEAD is fb398b6, the tests/ tree is byte-identical to 2d04ca0, main is 3 ahead of origin with nothing pushed. The 187-test full-suite count and the compile checks are the producer's claims, not mine.
- Regression in `scripts/cost-query.sh:124`: filters are now baked into REPORT_EXTRA and REPORT_FROM once at startup, but session-cost sets SESSION_FILTER afterwards (line 674) and then calls usd_cost_query, which previously recomputed the filter lazily. When the session id is auto-detected from /tmp/interstat-session-id instead of `--session=`, the cost and by_model fields cover the whole database while the token fields stay session-scoped. The suite only exercises the explicit `--session=` form.
- Hook-row claiming in `scripts/analyze.py:283` is path-order sensitive. Transcripts are processed in sorted path order (random agent-<hex> ids), and each claims the oldest unparsed hook row of its subagent type, so two same-type subagents pair correctly only when hex order matches run order. A mispair attaches description, wall_clock_ms, bead_id and phase to the wrong token data. The fixture's aaa/bbb are ordered both ways, so the test cannot see this.
- The old fallback that claimed any unparsed hook row in the session was dropped. If a subagent's .meta.json is missing, agent_name falls back to the hash, no hook row matches, and the tokens land in a new row with no bead or phase while the hook row stays unparsed forever. The backfill receipt counts these under rows_without_source_path but nothing reconciles them.
- Lower-bound labeling only keys on cache_write_ttl_unreported. nonstandard_service_pricing (fast mode or non-standard service tier) and server_tool_charges_unpriced are stored in pricing_unknowns, yet cost.py still reports cost_estimate_complete true and cost_estimate_lower_bound false for those rows.
- `scripts/analyze.py:271` stamps PRAGMA user_version = 7 on every connection, so a future init-db.sh bump would be silently downgraded by each analyze run. Relatedly, the PostToolUse hook re-runs init-db.sh on every Task call because it compares user_version against "2" (pre-existing, but now guaranteed to fire).
- Window semantics diverge in mixed databases: breakdown rows filter by day inclusive, legacy rows by timestamp, and ttl_unreported_rows is windowed by run timestamp rather than day. Edge-of-window totals and the lower-bound count can disagree.
- The lenient extractor silently drops malformed lines and the old skip for files over 50% corrupt is gone, so damaged transcripts now ingest partially with no log line. Also, total_api_equivalent in the cost.py JSON is now an unrounded float while known_cost_subtotal stays rounded, a small interface change for downstream consumers such as ic cost.
