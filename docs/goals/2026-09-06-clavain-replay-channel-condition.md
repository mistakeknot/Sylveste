# Goal condition — Clavain replay channel (ratified 2026-09-06)

mk asked "please execute the above /goal" on 2026-09-06, naming the Next-goal block that closed goal 7be37994. Goal-shaped rendering by the orchestrating session, not mk's ruling in mk's voice.

```
/goal Make both halves of the replay channel real: the validation seat runs the plan's Verification or refuses to rule, and exact contracts are applied and replayed by tool.

OUTCOME: A Pattern F verdict rests on an executed second run again. Verified by: `dispatch.sh --role validation` resolves to a seat that can run the plan's Verification block read-only (Bash allowed on the checkout, the plan path readable wherever it lives, file mutation still disallowed) and that emits `VERDICT: UNRUN` instead of PASS or FAIL whenever a Verification command could not execute, with the register row carrying that kind; `plan-gauge-lint.py --apply <plan> --repo-root <repo>` applies an exact contract's edit pairs and Create blocks and runs its Verify fences, exiting non-zero on the first mismatch, so an exact plan needs no executor model; the contracts doc says which contract goes to a model and which to the tool; demonstrated on one exact plan and one brief against a live checkout, every row read back from the register. Stop after 10 turns.

GATE: the seat change is proven by a validator that fails a deliberately broken checkout, not by one that passes a good one; no hand trace counts as a replay anywhere in the run; role-resolved dispatch, producer and validator on different models; absolutes per attempt; the gauge rules stay as they are.

DONE WHEN: Sylveste-soj7 closed against a tested Clavain commit; `--apply` shipped with bats coverage and the contracts doc updated; clavain published from zklw and refreshed here with a content diff; the two live demonstrations journaled with their register rows.

OUT: interstat (Sylveste-y8xx residuals stay open), the Codex executor seat, calibration and quarantine policy, orchestrate.py timeouts (mk-kj2m), the audit hunks (your ruling).
```
