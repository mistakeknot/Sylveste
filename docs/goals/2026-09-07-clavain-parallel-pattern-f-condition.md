OUTCOME: A run file with `max_parallel: N` dispatches up to N items concurrently. Verified by: each item reserves its plan's declared files through interlock before its worktree is cut and releases them on merge or park, an item whose reservation conflicts waits rather than runs, merges land in completion order with a rebase attempt and a park on conflict, every dispatch still runs in its own process group with the run's timeout, register rows keep the run and item ids, the closing packet lists per item its wait time, its run time and its merge outcome, and pattern-f-meter.py reports the run's wall time and main-thread turns against the same items run sequentially; the interlock commit lock works from a worktree (mk-gly2); demonstrated on three independent backlog items in one run. Stop after 10 turns.
