Sylveste — measure the whole session opening, then remove what it pays for and does not use

OUTCOME: A session on this rig opens with materially less fixed context than the 81,295 tokens measured on 2026-09-16, and every cut that got it there is backed by an eval showing the affected skills still trigger and still behave. Today only plugin advertisement text is instrumented, which is under a tenth of the opening, so the rig has been optimizing the part it can see. When this is done the whole opening is measurable on demand on both machines, and the published number is reproducible by re-running the meter.

GATE 1 — instrument trust: two meters must agree before any inventory is trusted — one that reads what an interactive session actually loaded, one that probes a fresh session cheaply. Print mode omits interactive-only tool schemas, so a probe delta does not transfer to an interactive session on its own. Report the residual between the meters and the usage totals the tool itself reports, as a number.

GATE 2 — quality before savings: no cut lands before an eval covers the skills it touches, run at a pinned judge model and run count, with the with/without delta recorded. Where a case cannot be written for a cut, say so and leave that cut unlanded.

GATE 3 — waste before capability: cuts that remove duplication or dormancy land ahead of any cut that removes a capability, and each lands with a measured before and after on a fresh session rather than an estimate.

GATE 4 — capability is mine to rule: for each switch that buys tokens by removing a feature, and for each dormant server, bring me what it costs, what it removes, and what the evals say, and recommend one per lever. QUESTION: which levers come off, and on which machines?

GATE 5 — both machines: what lands on the Mac lands on the dev server through the existing sync path, and the budget check reports the new number on both.

DONE WHEN:
- the meter is committed with tests, and re-running it against a fresh session reproduces the opening figure it reported
- the eval suite is committed, runs green, and its results artifact is in the repo
- every landed cut names its measured saving and the eval run that cleared it
- a fresh interactive session's opening is re-measured and published as the new baseline, with the delta from 81,295 stated
- the capability levers are written up for a ruling and remain unapplied
- the beads this supersedes are closed or re-scoped, and no duplicate tracking bead is created
- or stop after 40 turns and report the ranked inventory with whatever deltas are proven

OUT:
- applying any capability lever before the GATE 4 ruling
- winning bytes by deleting trigger words from descriptions; the eval is the judge, not the byte count
- client-facing and Notion configuration
- a second task system; tracking stays in the existing beads
