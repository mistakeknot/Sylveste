---
date: 2026-04-18
session: 36ddfec8
topic: B4 shipped (v0.2.64), MCP-efficiency workstream filed
beads: [sylveste-s46z, sylveste-116u, sylveste-krop, sylveste-x6e4, sylveste-35x5, sylveste-06yf, sylveste-7505]
---

## Session Handoff — 2026-04-18 B4 shipped + MCP-efficiency workstream filed

### Directive
> Your job is to execute **spike C** (`sylveste-116u`) — measure MCP cold-start breakdown so the C+D strategy has data. Start by inventorying the MCP launchers Claude Code fires in this env (enumerate plugin `.claude-plugin/plugin.json` `mcpServers` entries across `~/.claude/plugins/cache/interagency-marketplace/*/*/` and any user-scope servers in `~/.claude/settings.json`). Then time each launcher in two dimensions: (a) process-to-stdin-ready via `time` + `/proc/PID/status` RSS, (b) `initialize` handshake RTT via a minimal stdio client. Separate env/module-load cost from protocol-init cost. Deliverable: `/home/mk/projects/Sylveste/docs/research/mcp-cold-start-breakdown-2026-04-18.md` — ranked table (server, language, cold-start ms, RSS MB, initialize RTT ms) + recommendation memo (pre-warm venvs? frozen deps? `python -m` direct entry?). Bead is **claimed in_progress** — refresh heartbeat periodically: `clavain-cli bead-heartbeat sylveste-116u`.
- Beads in_progress: `sylveste-116u` (spike C, claimed this session)
- Track: C+D primary, E background, A deferred
- Dependency chain: `sylveste-7505` (A — full consolidation) now depends-on `sylveste-x6e4` (E1 multi-mcpServers spike) + `sylveste-35x5` (E2 interlens TS SDK upgrade) + `sylveste-06yf` (E3 interfer decision). All three must land before A can be revived.
- Fallback if spike C blocks: pick up `sylveste-krop` (spike D, lazy MCP enablement design — pure doc, no measurement) instead.

### Dead Ends
- **`ic publish --patch` first attempt** — stale publish lock `pub-9jb6nf6u` in `.clavain/intercore.db:publish_state`. Same dead-end the prior handoff warned about. Fix: `python3 -c "import sqlite3; c=sqlite3.connect('.clavain/intercore.db'); c.execute(\"DELETE FROM publish_state WHERE plugin='interflux' AND phase NOT IN ('done','idle')\"); c.commit()"`. `ic publish status` does not show these rows; `bd doctor` does not flag them. sylveste-t615 tracks the idempotency fix.
- **Resurrecting `sylveste-7505` full consolidation as-written** — flux-drive strategy review on 2026-04-10 returned **REVISE_PRD** with 12 P0 findings, 4 independent reviewers converging. Top blockers: (P0-1) PRD claims interflux proves multi-`mcpServers` support but every `interverse/*/plugin.json` has exactly ONE entry; (P0-2) adapter contract `server.tool(name, handler, schema=...)` is FastMCP idiom — 5/6 in-scope Python plugins use low-level `mcp.server.Server` where a second `@list_tools` decoration overwrites the first; (P0-7) `../*/adapters/` filesystem discovery works in `--plugin-dir` mode but silently loads zero adapters from marketplace cache layout. Do NOT attempt to execute F1–F6 beads (`sylveste-h4mw, -amcz, -6m1k, -lbvq, -39p5, -jsyc`) without revising the PRD. Synthesis: `/home/mk/projects/Sylveste/docs/research/flux-drive/2026-04-10-interserve-consolidated-mcp/synthesis.md`.

### Context
- **Live evidence validating MCP-efficiency thesis (but not the current blueprint):** 118 MCP-ish processes on dev box at session start; 6 of ~14 announced MCP servers disconnected mid-session 2026-04-18 (context7, intercache, interdeep, interrank, intersearch, tldr-swinton). ≈40% in-session failure rate — architectural fragility is real-time observable.
- **Why C+D chosen over A:** A (full consolidation per 7505 blueprint) assumes multi-`mcpServers` support and FastMCP-native plugins — both false. C (pre-warm / direct-entry for `uv run` cold-start) and D (lazy/per-project MCP enablement) attack the *actual* cost (wall-time + unused-server load) without touching plugin architecture. Net effect may exceed what consolidation would have delivered, at fraction of the cost. A stays on ice until E1/E2/E3 land.
- **Spike C caveat (from P0-12 of synthesis):** original brainstorm committed to process count (19→11) but never to cold-start wall time or RSS — a consolidated server could cold-start *slower* than legacy sum due to 6 chained adapter imports. Spike C's deliverable must report wall-time, not process count, so a future 7505 revive has an actual gate.
- **interflux v0.2.64 runtime state:** `/home/mk/projects/Sylveste/interverse/interflux/.clavain/interspect/interspect.db` is tracked-but-gitignored and churns on every interspect event. `ic publish` refuses to run with dirty tree. Before any future `ic publish`: `git stash push -- .clavain/interspect/interspect.db`; after: `git stash pop`. Also check `/home/mk/projects/Sylveste/core/marketplace/` is not mid-rebase (clean this session).
- **Pre-existing test failures unchanged by B4** (verify these match your baseline before concluding a regression): structural suite 7 failures — `test_version_gating_with_severity`, `test_skips_invalid_names`, `test_launch_uses_interknow_mcp`, `test_launch_uses_interknow_collection`, `test_slicing_has_report_template`, `test_skill_has_diff_profile`, `test_launch_has_diff_to_review_section`; `tests/test-budget.sh` 1 failure — `AGENTS.md missing Measurement Definitions`.
- **Beads deliberately NOT filed this session** (file if/when 7505 is revived): P0-8 `mcp__plugin_` tool-identifier audit across commands/skills/agents; tldr-swinton correction (brainstorm said Go, is actually Python with `mcp>=1.0.0` dependency — affects F4 scope).
- **Key paths for in-progress work:** spike C target `/home/mk/projects/Sylveste/docs/research/mcp-cold-start-breakdown-2026-04-18.md` (does not exist yet); 7505 brainstorm `/home/mk/projects/Sylveste/docs/brainstorms/2026-04-08-interserve-consolidated-mcp-brainstorm.md`; 7505 PRD `/home/mk/projects/Sylveste/docs/prds/2026-04-10-interserve-consolidated-mcp.md`; 7505 synthesis (12 P0s) `/home/mk/projects/Sylveste/docs/research/flux-drive/2026-04-10-interserve-consolidated-mcp/synthesis.md`; interflux blueprint `/home/mk/projects/Sylveste/docs/plans/2026-04-18-interflux-improvement-plan.md` (B4 done, B5/B6 next per prior handoff).
- **MEMORY.md still at 181/120 lines over budget** — `/intermem:tidy` not run this session. Pick up as side-quest or defer.
