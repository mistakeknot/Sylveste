---
artifact_type: plan
bead: Sylveste-ttf
stage: design
requirements:
  - F1: Structured verify blocks
  - F2: Plan-level must-haves
  - F3: Requirement traceability
---
# Plans-as-Prompts Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use clavain:executing-plans to implement this plan task-by-task.

**Bead:** Sylveste-ttf
**Phase:** planned (as of 2026-03-08T09:33:36Z)
**Goal:** Add structured verification, goal-backward must-haves, and requirement traceability to Clavain plan format.

**Architecture:** Three additive changes to two skill files (writing-plans, executing-plans). All features are backward-compatible — plans without the new sections work exactly as before. No new files, no new dependencies, no library extraction needed (these are prompt template changes, not code).

**Tech Stack:** Markdown skill files (SKILL.md)

**Prior Learnings:**
- `os/clavain/docs/solutions/2026-02-19-extract-skillmd-inline-logic-to-library.md` — SKILL.md files are prompt templates describing workflows, not implementations. Don't inline parsing code; describe what the agent should do.
- `docs/solutions/patterns/hybrid-cli-plugin-architecture-20260223.md` — Skills orchestrate multi-step flows. Keep the surface minimal.

---

## Must-Haves

**Truths** (observable behaviors):
- Plans generated by writing-plans include `<verify>` blocks in the task template
- Plans generated by writing-plans include a `## Must-Haves` section with truths/artifacts/key_links
- Plans generated by writing-plans include `requirements` in YAML frontmatter
- Executing-plans parses and runs `<verify>` blocks after each task, reporting pass/fail
- Executing-plans validates must-haves after all tasks complete, reporting results
- Plans without the new sections execute identically to before (backward compatible)

**Artifacts** (files that must exist):
- `os/clavain/skills/writing-plans/SKILL.md` contains verify block template, must-haves section, requirements frontmatter
- `os/clavain/skills/executing-plans/SKILL.md` contains verify parsing instructions and must-have validation instructions

**Key Links** (critical wiring):
- writing-plans task template `<verify>` format matches what executing-plans expects to parse
- executing-plans verify failure routes through deviation Rule 1 (auto-fix)
- Must-haves section position in plan (after header, before tasks) is consistent between writer and executor

---

### Task 1: Add structured fields to writing-plans SKILL.md

**Files:**
- Modify: `os/clavain/skills/writing-plans/SKILL.md`

**Step 1: Add `requirements` to frontmatter template**

In the "Plan Document Header" section, add `requirements` to the YAML frontmatter block:

```yaml
---
artifact_type: plan
bead: <CLAVAIN_BEAD_ID or "none">
stage: design
requirements:
  - F1: <feature name from PRD>
  - F2: <feature name from PRD>
---
```

Add a brief note after the frontmatter template:

```markdown
The `requirements` field links plan tasks to PRD feature IDs. Use the feature numbering from the PRD (F1, F2, etc.). This field is optional — omit it when no PRD exists.
```

**Step 2: Add Must-Haves section to plan header**

After the `**Prior Learnings:**` line (and before the `---` separator and first task), add a new section to the plan header template:

```markdown
## Must-Haves

Derive these using goal-backward methodology: "What must be TRUE for the goal to be achieved?"

**Truths** (observable behaviors — verifiable by using the application):
- [User can do X / System responds with Y / Data persists across Z]

**Artifacts** (files that must exist with specific exports):
- [`path/to/file.py`] exports [`function_name`, `class_name`]

**Key Links** (critical connections where breakage causes cascading failures):
- [Component A calls Component B before Component C]
```

Add guidance after the template:

```markdown
**Deriving must-haves:**
1. State the goal as an outcome, not a task ("Working chat interface", not "Build chat components")
2. List 3-7 truths from the user's perspective ("User can see messages", "Messages persist across refresh")
3. For each truth, identify required artifacts (files, exports, types)
4. For each artifact, identify key links (what must be connected for it to function)
5. Focus key_links on where breakage causes cascading failures

Must-haves are optional. Omit for trivial plans (complexity 1-2) or when the goal is self-evident.
```

**Step 3: Add `<verify>` block to task template**

In the "Task Structure" section, add a `<verify>` block after the last step (Step 5: Commit) in the task template:

````markdown
**Step 5: Commit**

```bash
git add tests/path/test.py src/path/file.py
git commit -m "feat: add specific feature"
```

<verify>
- run: `pytest tests/path/test.py -v`
  expect: exit 0
- run: `python -c "from src.module import func; print(func('test'))"`
  expect: contains "expected_output"
</verify>
````

Add guidance after the task template:

```markdown
**Writing verify blocks:**
- Place `<verify>` at the end of each task, after the final step
- Each entry has `run:` (exact command) and `expect:` (pass condition)
- Two matchers: `exit 0` (command succeeds), `contains "string"` (output includes substring)
- Verify blocks are optional — omit for tasks that are purely documentation or configuration
- The executor runs these automatically after completing all task steps
```

**Step 4: Commit**

```bash
cd os/clavain && git add skills/writing-plans/SKILL.md && git commit -m "feat(writing-plans): add verify blocks, must-haves, and requirements to plan format"
```

<verify>
- run: `grep -c '<verify>' os/clavain/skills/writing-plans/SKILL.md`
  expect: exit 0
- run: `grep -c 'Must-Haves' os/clavain/skills/writing-plans/SKILL.md`
  expect: exit 0
- run: `grep -c 'requirements:' os/clavain/skills/writing-plans/SKILL.md`
  expect: exit 0
</verify>

---

### Task 2: Add verify parsing and must-have validation to executing-plans SKILL.md

**Files:**
- Modify: `os/clavain/skills/executing-plans/SKILL.md`

**Step 1: Add verify block parsing after task completion**

After Step 2B ("Direct Execution") and before Step 3 ("Report"), add a new section:

```markdown
### Step 2D: Post-Task Verification

After completing each task (in any execution mode), check for a `<verify>` block at the end of the task:

1. **Parse the verify block:** Look for `<verify>...</verify>` after the task's last step. Extract each `- run:` / `expect:` pair.
2. **Run each verification command** in order.
3. **Check results:**
   - `expect: exit 0` — command must exit with code 0
   - `expect: contains "string"` — command output must include the quoted string
4. **On success:** Log "✓ Verify passed for Task N" and continue.
5. **On failure:** Treat as deviation Rule 1 (auto-fix bug). Review the task implementation, fix the issue, re-run the verification. Apply the 3-attempt limit — if verify still fails after 3 attempts, log the failure and continue to the next task.
6. **No verify block:** Skip silently — this is backward compatible. Log nothing.
```

**Step 2: Add must-have validation after all tasks**

After Step 4 ("Continue") and before Step 5 ("Complete Development"), add a new section:

```markdown
### Step 4B: Must-Have Validation

After all tasks complete, check for a `## Must-Haves` section in the plan header:

1. **Truths:** For each truth listed, verify it's observable. If the truth references a command or URL, run it. If it describes user behavior, check that the relevant code path exists.
2. **Artifacts:** For each artifact, verify the file exists and exports the listed symbols. Use grep or read the file to confirm.
3. **Key Links:** For each key link, verify the connection exists in the source code (e.g., "Component A calls Component B" → grep for the import and function call).

**Report must-have results** in the completion summary:

```
Must-Have Validation:
  ✓ Truths: 3/3 verified
  ✓ Artifacts: 2/2 exist with exports
  ✗ Key Links: 1/2 — Registration endpoint missing validate_email call
```

If any must-have fails: report the failure but do NOT block completion. Must-haves are advisory — the user decides whether to fix them before shipping.

If no Must-Haves section exists: skip silently (backward compatible).
```

**Step 3: Update batch report format**

In Step 3 ("Report"), add verify and must-have results to the batch report template:

```markdown
- Show verification output (pass/fail per task from `<verify>` blocks)
- Show must-have validation results (if present)
```

**Step 4: Commit**

```bash
cd os/clavain && git add skills/executing-plans/SKILL.md && git commit -m "feat(executing-plans): parse verify blocks and validate must-haves after execution"
```

<verify>
- run: `grep -c 'Post-Task Verification' os/clavain/skills/executing-plans/SKILL.md`
  expect: exit 0
- run: `grep -c 'Must-Have Validation' os/clavain/skills/executing-plans/SKILL.md`
  expect: exit 0
- run: `grep -c 'verify block' os/clavain/skills/executing-plans/SKILL.md`
  expect: exit 0
</verify>

---

### Task 3: Update compact skill variants (if they exist)

**Files:**
- Modify: `os/clavain/skills/writing-plans/SKILL-compact.md` (if exists)
- Modify: `os/clavain/skills/executing-plans/SKILL-compact.md` (if exists)

**Step 1: Check if compact variants exist**

```bash
ls os/clavain/skills/writing-plans/SKILL-compact.md os/clavain/skills/executing-plans/SKILL-compact.md 2>/dev/null
```

If neither file exists, skip this task entirely.

**Step 2: If writing-plans compact exists**

Add a condensed version of the verify block template, must-haves section, and requirements frontmatter. Keep it minimal — the compact version should mention the features exist and point to the full SKILL.md for guidance.

**Step 3: If executing-plans compact exists**

Add condensed instructions for verify block parsing and must-have validation. Reference the full SKILL.md for the complete protocol.

**Step 4: Commit** (only if changes were made)

```bash
cd os/clavain && git add skills/*/SKILL-compact.md && git commit -m "feat: update compact skill variants with verify/must-have support"
```

<verify>
- run: `ls os/clavain/skills/writing-plans/SKILL-compact.md 2>/dev/null && grep -c 'verify' os/clavain/skills/writing-plans/SKILL-compact.md || echo "no compact variant"`
  expect: exit 0
</verify>
