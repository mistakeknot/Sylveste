# Beads Configuration File
issue-prefix: "Demarch"

# Per-project Dolt server (isolated from other projects)
dolt.shared-server: false

sync-branch: "beads-sync"
sync.remote: "origin"
events-export: true

backup.enabled: true
backup.interval: 5m
backup.git-push: false
