# Generated by scripts/gen-gemini-commands.sh. Do not edit by hand.
description = "Remove a pattern from the routing override blacklist"
prompt = """
Generated from Demarch slash-command markdown.

- Source command: /interspect:interspect-unblock
- Source file: /home/mk/projects/Demarch/interverse/interspect/commands/interspect-unblock.md
- When instructions mention Gemini elicitation adapter, ask the user one concise question in chat and wait for the answer.
- When instructions mention activate_skill tool, use Gemini's activate_skill tool.


# Interspect Unblock

Remove a pattern from the blacklist so interspect can propose it again.

<unblock_target> #$ARGUMENTS </unblock_target>

## Execute

```bash
# Prefer own copy, fall back to Clavain cache, then monorepo dev path
INTERSPECT_LIB="${CLAUDE_PLUGIN_ROOT}/hooks/lib-interspect.sh"
if [[ ! -f "$INTERSPECT_LIB" ]]; then
    INTERSPECT_LIB=$(find ~/.gemini/plugins/cache -path '*/interspect/*/hooks/lib-interspect.sh' -o -path '*/clavain/*/hooks/lib-interspect.sh' 2>/dev/null | head -1)
fi
if [[ -z "$INTERSPECT_LIB" || ! -f "$INTERSPECT_LIB" ]]; then
    echo "Error: Could not locate hooks/lib-interspect.sh" >&2
    exit 1
fi
source "$INTERSPECT_LIB"
_interspect_ensure_db
DB=$(_interspect_db_path)

if ! _interspect_validate_agent_name "$AGENT"; then
    exit 1
fi
_interspect_unblacklist_pattern "$AGENT"
```

Report: "Unblocked {agent}. Interspect may propose this exclusion again if evidence warrants it." or "No blacklist entry found for {agent}."
"""
