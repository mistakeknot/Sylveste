# Generated by scripts/gen-gemini-commands.sh. Do not edit by hand.
description = "Force regeneration of a specific document regardless of drift score"
prompt = """
Generated from Demarch slash-command markdown.

- Source command: /interwatch:refresh
- Source file: interverse/interwatch/commands/refresh.md
- When instructions mention Gemini elicitation adapter, ask the user one concise question in chat and wait for the answer.
- When instructions mention activate_skill tool, use Gemini's activate_skill tool.


# Force Refresh

Force-refresh a specific watchable regardless of its drift score.

## Arguments

<refresh_args> #$ARGUMENTS </refresh_args>

Parse the doc name from the arguments. Valid names come from `config/watchables.yaml`.

If no argument provided, use Gemini elicitation adapter to ask which doc to refresh, listing available watchables.

Then invoke the `doc-watch` skill in **refresh** mode with the specified watchable name.

This bypasses detection and assessment — it directly invokes the generator for the specified doc.

Use the activate_skill tool to invoke `interwatch:doc-watch` with the instruction: "Force refresh the [doc-name] watchable."
"""
