# Generated by scripts/gen-gemini-commands.sh. Do not edit by hand.
description = "Install or update the interline statusline script"
prompt = """
Generated from Demarch slash-command markdown.

- Source command: /interline:statusline-setup
- Source file: /home/mk/projects/Demarch/interverse/interline/commands/statusline-setup.md
- When instructions mention Gemini elicitation adapter, ask the user one concise question in chat and wait for the answer.
- When instructions mention activate_skill tool, use Gemini's activate_skill tool.


# Statusline Setup

Install the interline statusline renderer into `~/.gemini/statusline.sh` and configure Claude Code to use it.

## Steps

1. Find the interline plugin directory in the cache:
```bash
INTERLINE_DIR=$(ls -d ~/.gemini/plugins/cache/*/interline/*/scripts/install.sh 2>/dev/null | head -1 | xargs dirname | xargs dirname)
if [ -z "$INTERLINE_DIR" ]; then
  echo "ERROR: interline plugin not found in cache. Install with: claude plugin install interline@interagency-marketplace"
  exit 1
fi
echo "Found interline at: $INTERLINE_DIR"
```

2. Run the install script:
```bash
bash "$INTERLINE_DIR/scripts/install.sh"
```

3. Verify the installation:
```bash
echo '{"model":{"display_name":"Test"},"workspace":{"current_dir":"/test"}}' | ~/.gemini/statusline.sh
```

4. Report success or failure to the user. The statusline will be active in the next Claude Code session.

## Configuration

The install creates `~/.gemini/interline.json` with defaults. Users can customize:

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| `colors.interserve` | array or number | `[210,216,228,157,111,183]` | Per-letter rainbow (array) or single color (number) for the Interserve label |
| `colors.dispatch` | number | _(none)_ | ANSI 256-color for dispatch status text |
| `colors.bead` | number | _(none)_ | ANSI 256-color for bead context text |
| `colors.phase` | number | _(none)_ | ANSI 256-color for workflow phase text |
| `colors.branch` | number | _(none)_ | ANSI 256-color for git branch name |
| `layers.dispatch` | boolean | `true` | Show Codex dispatch state |
| `layers.bead` | boolean | `true` | Show bead context (ID + phase) |
| `layers.phase` | boolean | `true` | Show workflow phase from transcript |
| `layers.interserve` | boolean | `true` | Show interserve mode indicator |
| `colors.coordination` | number | `214` | ANSI 256-color for coordination status text |
| `layers.coordination` | boolean | `true` | Show coordination status from interlock signals (requires `INTERMUTE_AGENT_ID`) |
| `labels.interserve` | string | `"Interserve"` | Text for the interserve mode rainbow label |
| `labels.dispatch_prefix` | string | `"Interserve"` | Prefix before dispatch task name |
| `format.separator` | string | `" \| "` | Separator between status segments |
| `format.branch_separator` | string | `":"` | Separator between project name and branch |

All fields are optional. Missing keys use built-in defaults. Delete the file to reset everything.

### Examples

Disable bead context and make phase labels red:
```json
{
  "layers": { "bead": false },
  "colors": { "phase": 196 }
}
```

Use a single teal color for the Interserve label:
```json
{
  "colors": { "interserve": 44 }
}
```
"""
