# Generated by scripts/gen-gemini-commands.sh. Do not edit by hand.
description = "Run smoke tests against a running app — detect dev server, walk critical user journeys, report results"
prompt = """
Generated from Demarch slash-command markdown.

- Source command: /clavain:smoke-test
- Source file: os/Clavain/commands/smoke-test.md
- When instructions mention Gemini elicitation adapter, ask the user one concise question in chat and wait for the answer.
- When instructions mention activate_skill tool, use Gemini's activate_skill tool.


# /clavain:smoke-test

<smoke_test_input> #$ARGUMENTS </smoke_test_input>

## Step 1: Detect App

1. **URL argument** → use directly
2. **Port argument** → `http://localhost:<port>`
3. **No argument** → auto-detect:
   ```bash
   for port in 3000 3001 5173 5174 4321 8000 8080 8888; do
     curl -s -o /dev/null -w "%{http_code}" "http://localhost:$port" 2>/dev/null | grep -q "200\|301\|302" && echo "Found: http://localhost:$port"
   done
   ```
4. **Nothing found** → check for TUI app (main.go, Cargo.toml with terminal deps) and use TUI Vision

Report detected target before proceeding.

## Step 2: Find User Journeys

Check in order: `docs/prds/*.md` (acceptance criteria), `docs/plans/*.md` (test scenarios), `README.md` (usage examples). If nothing found, infer 3-5 basic journeys (load homepage, navigate, submit form, etc.). List journeys and proceed.

## Step 3: Run Smoke Tests

**Web apps** — use `webapp-testing` skill if available, else `WebFetch`:
- Main page: 200 + expected content
- Key routes: no 500s or blank pages
- Forms: required fields and submit buttons exist
- APIs: key endpoints respond with correct shape

**TUI apps** — use `tuivision` MCP tools:
- `spawn_tui` → `get_screenshot` (verify render) → send key inputs → `wait_for_text` (verify flows) → close session

Record pass/fail, what was checked, and error details per journey.

## Step 4: Report Results

```
Smoke Test Results
==================
App: <URL or binary>
Journeys tested: <N>

✓ <journey 1> — <what was verified>
✗ <journey 2> — <error details>

Result: <N>/<total> passed
```

Failing journeys: provide actionable details (HTTP status, missing element, error message).

**Integration:** called automatically by `/clavain:work` after execution when a dev server is detectable.
"""
