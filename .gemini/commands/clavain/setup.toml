# Generated by scripts/gen-gemini-commands.sh. Do not edit by hand.
description = "Bootstrap Clavain for the active runtime (Codex or Claude Code) and verify health"
prompt = """
Generated from Demarch slash-command markdown.

- Source command: /clavain:setup
- Source file: os/Clavain/commands/setup.md
- When instructions mention Gemini elicitation adapter, ask the user one concise question in chat and wait for the answer.
- When instructions mention activate_skill tool, use Gemini's activate_skill tool.


# Clavain Modpack Setup

Bootstrap the Clavain engineering rig. Re-run to verify/repair. Use `/clavain:project-onboard` for project setup (beads, CLAUDE.md, docs, observability) — this command sets up Clavain itself.

## Arguments

<setup_args> #$ARGUMENTS </setup_args>

`--check-only`: verify only, no changes. `--scope=interlock`: intermute service only. `--scope=clavain` (default): full modpack. `--scope=all`: force both.

## Step 0: Route by Runtime (critical)

**Codex CLI:** Do NOT run `claude plugin ...`. Run the Codex bootstrap script, then stop:

```bash
SCRIPT_PATH="${CLAVAIN_SOURCE_DIR:-$HOME/.codex/clavain}/scripts/install-codex-interverse.sh"
if [[ ! -f "$SCRIPT_PATH" ]]; then
  for candidate in \
    "$PWD/os/clavain/scripts/install-codex-interverse.sh" \
    "$HOME/projects/Sylveste/os/clavain/scripts/install-codex-interverse.sh"; do
    if [[ -f "$candidate" ]]; then SCRIPT_PATH="$candidate"; break; fi
  done
fi
if [[ -z "$SCRIPT_PATH" || ! -f "$SCRIPT_PATH" ]]; then
  echo "Error: install-codex-interverse.sh not found. Run /clavain:codex-bootstrap first."; exit 1
fi
# check-only: bash "$SCRIPT_PATH" doctor --json
# otherwise:  bash "$SCRIPT_PATH" install && bash "$SCRIPT_PATH" doctor --json
ls ~/.agents/skills/
```

**Claude Code:** continue with Steps 1+.

## Step 1: Verify Clavain (Claude Code path)

`ls ~/.gemini/plugins/cache/interagency-marketplace/clavain/*/skills/using-clavain/SKILL.md`

## Step 2: Install Required & Recommended Plugins

`modpack-install.sh` reads `agent-rig.json` at runtime — no hardcoded lists to go stale.

```bash
CLAVAIN_DIR=$(dirname "$(ls ~/.gemini/plugins/cache/interagency-marketplace/clavain/*/agent-rig.json 2>/dev/null | head -1)")
INSTALL_SCRIPT="${CLAVAIN_DIR}/scripts/modpack-install.sh"
```

- `--check-only`: `result=$("$INSTALL_SCRIPT" --dry-run --quiet)`
- Otherwise: `result=$("$INSTALL_SCRIPT" --quiet)` — installs core + required + recommended, disables conflicts

Parse result JSON and report: **installed** (list), **already_present** (count), **failed** (warn each), **disabled**, **optional_available** (→ Step 2b).

If `$INSTALL_SCRIPT` or `jq` unavailable, fall back to manual lists below.

**Language servers** — ask via Gemini elicitation adapter which languages to enable:
<!-- agent-rig:begin:install-infrastructure -->
- Go → `claude plugin install gopls-lsp@claude-plugins-official`
- Python → `claude plugin install pyright-lsp@claude-plugins-official`
- TypeScript → `claude plugin install typescript-lsp@claude-plugins-official`
- Rust → `claude plugin install rust-analyzer-lsp@claude-plugins-official`
<!-- agent-rig:end:install-infrastructure -->

## Step 2b: Optional Plugins

Get uninstalled optionals: `optional=$("$INSTALL_SCRIPT" --dry-run --quiet --category=optional | jq -r '.optional_available[]')`

Present via Gemini elicitation adapter (multi-select). Install selected: `claude plugin install <plugin>`

Fallback list if script unavailable:
<!-- agent-rig:begin:install-optional -->
- `interfluence@interagency-marketplace` — Voice profile and style adaptation
- `interject@interagency-marketplace` — Ambient discovery and research engine (MCP)
- `interstat@interagency-marketplace` — Token efficiency benchmarking
- `interkasten@interagency-marketplace` — Notion sync and documentation
- `interlens@interagency-marketplace` — Cognitive augmentation lenses (MCP)
- `intersearch@interagency-marketplace` — Shared embedding and Exa search library
- `interpub@interagency-marketplace` — Plugin publishing automation
- `tuivision@interagency-marketplace` — TUI automation and visual testing (MCP)
- `intermux@interagency-marketplace` — Agent activity visibility and tmux monitoring (MCP)
- `interleave@interagency-marketplace` — Deterministic skeleton + LLM islands pattern
- `interlearn@interagency-marketplace` — Cross-repo institutional knowledge index
- `intercache@interagency-marketplace` — Cross-session semantic cache (MCP)
- `interchart@interagency-marketplace` — Interactive ecosystem diagram generator
<!-- agent-rig:end:install-optional -->

## Step 3: Disable Conflicting Plugins

Conflicts are handled automatically by `modpack-install.sh` — skip this step if automated install ran. Manual fallback:

<!-- agent-rig:begin:disable-conflicts -->
```bash
claude plugin disable code-review@claude-plugins-official
claude plugin disable pr-review-toolkit@claude-plugins-official
claude plugin disable code-simplifier@claude-plugins-official
claude plugin disable commit-commands@claude-plugins-official
claude plugin disable feature-dev@claude-plugins-official
claude plugin disable claude-md-management@claude-plugins-official
claude plugin disable frontend-design@claude-plugins-official
claude plugin disable hookify@claude-plugins-official
claude plugin disable superpowers@superpowers-marketplace
claude plugin disable compound-engineering@every-marketplace
```
<!-- agent-rig:end:disable-conflicts -->

## Step 4: Verify MCP Servers

- **context7** — declared in Clavain's plugin.json (automatic)
- **qmd** — `command -v qmd && qmd status`. If missing: "Install from https://github.com/tobi/qmd for semantic search across project docs."
- **Oracle** (optional) — `command -v oracle && pgrep -f "Xvfb :99"`. Report status; not installed is OK.

## Step 5: Initialize Beads (if not configured)

`ls .beads/ 2>/dev/null` — if absent, ask: "Initialize beads issue tracking for this project? (bd init)"

## Step 5b: Build Intercore Kernel (ic)

`command -v ic && ic health`

If not found or unhealthy:

1. `go version` — if missing, warn "Go >= 1.22 required: https://go.dev/dl/" and skip.

2. Find intercore source (check in order):
```bash
ls core/intercore/cmd/ic/main.go 2>/dev/null
ls ../core/intercore/cmd/ic/main.go 2>/dev/null
ls ../../core/intercore/cmd/ic/main.go 2>/dev/null
ls ~/projects/Sylveste/core/intercore/cmd/ic/main.go 2>/dev/null
```
If not found, warn "Clone https://github.com/mistakeknot/Sylveste and re-run setup." and skip.

3. Build and install:
```bash
mkdir -p ~/.local/bin
go build -C <intercore_source_dir> -mod=readonly -o ~/.local/bin/ic ./cmd/ic
```

4. `ic init && ic health`

5. PATH check: `echo "$PATH" | tr ':' '\n' | grep -qx "$HOME/.local/bin"` — if missing, warn "Add ~/.local/bin to PATH: `export PATH=\"$HOME/.local/bin:$PATH\"`"

If already present and healthy: report "ic kernel: healthy (version X.Y.Z)"

## Step 5c: Configure Statusline (interline)

```bash
INTERLINE_INSTALL=$(ls ~/.gemini/plugins/cache/*/interline/*/scripts/install.sh 2>/dev/null | head -1)
if [ -n "$INTERLINE_INSTALL" ]; then
  bash "$INTERLINE_INSTALL"
else
  echo "interline not installed — statusline will not be configured"
fi
```

Installs `~/.gemini/statusline.sh`, creates `~/.gemini/interline.json` (rainbow "Clavain" label, "Dispatch" prefix, bead_query/coordination disabled), wires `settings.json`.

## Step 6: Verify Configuration

<!-- agent-rig:begin:verify-script -->
```bash
# Resolve script path relative to plugin cache (works from any cwd)
VERIFY_SCRIPT="$(dirname "$(ls "$HOME/.gemini/plugins/cache"/*/clavain/*/scripts/verify-config.sh 2>/dev/null | head -1)")/verify-config.sh"
if [[ -x "$VERIFY_SCRIPT" ]]; then
    bash "$VERIFY_SCRIPT"
else
    echo "ERROR: verify-config.sh not found in plugin cache. Try reinstalling clavain."
fi
```
<!-- agent-rig:end:verify-script -->

Then check MCP servers and companions:

```bash
echo "=== MCP Servers ==="
echo "context7: $(ls ~/.gemini/plugins/cache/*/context7/*/plugin.json 2>/dev/null | head -1 >/dev/null && echo 'OK' || echo 'MISSING')"
echo "qmd: $(command -v qmd >/dev/null 2>&1 && echo 'installed' || echo 'not installed')"

echo "=== Companions ==="
echo "codex dispatch: $(ls ~/.gemini/plugins/cache/interagency-marketplace/clavain/*/scripts/dispatch.sh 2>/dev/null | head -1 >/dev/null && echo 'OK' || echo 'MISSING')"
echo "interline: $(ls ~/.gemini/plugins/cache/*/interline/*/scripts/statusline.sh 2>/dev/null | head -1 >/dev/null && echo 'installed' || echo 'not installed')"
echo "oracle: $(command -v oracle >/dev/null 2>&1 && echo 'installed' || echo 'not installed')"
echo "interlock: $(ls ~/.gemini/plugins/cache/*/interlock/*/scripts/interlock-register.sh 2>/dev/null | head -1 >/dev/null && echo 'installed' || echo 'not installed')"
echo "beads: $(ls .beads/ 2>/dev/null | head -1 >/dev/null && echo 'configured' || echo 'not configured')"
echo "ic kernel: $(command -v ic >/dev/null 2>&1 && ic health >/dev/null 2>&1 && echo 'healthy' || echo 'not available')"
```

## Step 7: Summary

```
Clavain Modpack Setup Complete

Required plugins:  [see verify output]
Conflicts disabled: [X/8 disabled]
Language servers:   [list enabled]
MCP servers:       context7 ✓ | qmd [status]
Infrastructure:    oracle [status]
Beads:             [status]
ic kernel:         [healthy/not available]

Next steps:
- Try `/clavain:brainstorm improve error handling in this project` for a quick demo
- Or run `/clavain:route [task]` for the adaptive workflow entry point
- Run `/tool-time:tool-time` to see tool usage analytics
- Run `/clavain:upstream-sync` to check for updates
```
"""
