# Generated by scripts/gen-gemini-commands.sh. Do not edit by hand.
description = "Disciplined bug investigation that enforces \"reproduce first, then diagnose\" — composes debugging agents into a structured workflow"
prompt = """
Generated from Demarch slash-command markdown.

- Source command: /clavain:repro-first-debugging
- Source file: os/Clavain/commands/repro-first-debugging.md
- When instructions mention Gemini elicitation adapter, ask the user one concise question in chat and wait for the answer.
- When instructions mention activate_skill tool, use Gemini's activate_skill tool.


# Repro-First Debugging

<bug_description> #$ARGUMENTS </bug_description>

**If empty:** Ask: "What's the bug? Include: what happened, what was expected, any error messages."

Load the `systematic-debugging` skill for the full debugging methodology.

## Phase 1: Reproduce

Spawn `bug-reproduction-validator`:
```
Task(bug-reproduction-validator): "Reproduce this bug: <bug_description>. Find minimal steps to trigger it reliably."
```

- **Success**: capture exact steps, proceed to Phase 2
- **Failure**: report to user (what was tried, what happened, what info is needed) and stop until user provides more context

## Phase 2: Find the Cause

Check for regression — spawn `interflux:research:git-history-analyzer`:
```
Task(interflux:research:git-history-analyzer): "This bug: <description>. When did it start? Find the most likely commit that introduced it. Focus on recent changes to: <affected files>"
```

- **Regression found**: focus debugging on that commit's changes
- **Not a regression**: follow `systematic-debugging` skill — form hypotheses, gather evidence, eliminate, test

## Phase 3: Fix and Verify

1. Implement fix
2. Verify reproduction steps no longer trigger the bug
3. Add regression test
4. Run full test suite

## Phase 4: Document

```markdown
## Bug Report
**Symptom:** [what the user observed]
**Root Cause:** [what was actually wrong]
**Regression:** [yes/no; if yes: commit <hash>]
**Fix:** [what was changed]
**Test:** [regression test added]
**Prevention:** [what prevents similar bugs]
```

If root cause reveals a systemic issue, suggest `/clavain:compound`.

## Agents

| Agent | Phase |
|-------|-------|
| `bug-reproduction-validator` | 1 — reliable reproduction |
| `interflux:research:git-history-analyzer` | 2 — find suspect commits |
| `systematic-debugging` skill | 2 — structured evidence collection |
| `interflux:research:repo-research-analyst` | 2 — unfamiliar code areas |

**Never skip reproduction. Always add a regression test.**
"""
