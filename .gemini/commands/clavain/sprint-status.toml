# Generated by scripts/gen-gemini-commands.sh. Do not edit by hand.
description = "Deep scan of sprint workflow state — session continuity, pipeline health, plan progress, beads, and recommendations"
prompt = """
Generated from Demarch slash-command markdown.

- Source command: /clavain:sprint-status
- Source file: os/Clavain/commands/sprint-status.md
- When instructions mention Gemini elicitation adapter, ask the user one concise question in chat and wait for the answer.
- When instructions mention activate_skill tool, use Gemini's activate_skill tool.


# Sprint Status

Deep on-demand scan of current project's sprint workflow state.

## How to Run

```bash
source "${CLAUDE_PLUGIN_ROOT}/hooks/sprint-scan.sh" && sprint_full_scan
```

Output is pre-formatted markdown — present directly.

## Sections

- **0. Coordination** — Multi-session coordination status (if Intermute active).
- **1. Session Continuity** — Handoff files in `.clavain/scratch/`. Read `handoff-latest.md` before starting new work if present.
- **1.5. Active Sprints** — Sprint beads in-progress: progress bar, artifact paths, claimed session.
- **2. Workflow Pipeline** — Artifact counts: brainstorms (`docs/brainstorms/*-brainstorm.md`), PRDs (`docs/prds/*.md`), plans (`docs/plans/*.md`).
- **3. Plan Progress** — Checklist completion per plan (`- [ ]`/`- [x]`). Plans <50% older than a day may be stalled.
- **4. Orphaned Brainstorms** — Brainstorms with no matching plan/PRD filename.
- **5. Beads Health** — `bd stats` + stale bead count (if beads available).
- **6. Skipped Phases** — Last 20 commits with no plan/bead/PR/review reference. Excludes chore/docs/ci.
- **7. Recommendations** — 1–3 prioritized next actions, or "Ready to work."
- **8. Sprint Completion Rate** — % sprints reaching `done` vs cancelled.

```bash
clavain-cli sprint-stats 2>/dev/null
```

Skip silently if unavailable. Vision target: >70% for complexity ≤3.

## After the Scan

| Finding | Action |
|---------|--------|
| Handoff found | Read with Read tool, resume that work |
| Orphaned brainstorms | Ask user: convert with `/clavain:write-plan` or close |
| Strategy gap | Suggest `/clavain:strategy` |
| Stale beads | List, ask to close or update |
| Low plan completion | Suggest `/clavain:work <plan>` |
| Everything clean | Confirm good shape, ask what to work on |
"""
