# Generated by scripts/gen-gemini-commands.sh. Do not edit by hand.
description = "Toggle interserve execution mode"
prompt = """
Generated from Demarch slash-command markdown.

- Source command: /clavain:clodex-toggle
- Source file: /home/mk/projects/Demarch/os/Clavain/commands/clodex-toggle.md
- When instructions mention Gemini elicitation adapter, ask the user one concise question in chat and wait for the answer.
- When instructions mention activate_skill tool, use Gemini's activate_skill tool.


# Interserve Mode Toggle

Run the toggle script and display its output:

```bash
SCRIPT_PATH=$(find ~/.gemini/plugins/cache -path '*/clavain/*/scripts/clodex-toggle.sh' 2>/dev/null | head -1)
[[ -z "$SCRIPT_PATH" ]] && SCRIPT_PATH=$(find ~/projects/Clavain -name clodex-toggle.sh -path '*/scripts/*' 2>/dev/null | head -1)
if [[ -z "$SCRIPT_PATH" ]]; then
  echo "Error: Could not locate clodex-toggle.sh" >&2
  exit 1
fi
PROJECT_DIR="$(pwd)" bash "$SCRIPT_PATH"
```
"""
