# Generated by scripts/gen-gemini-commands.sh. Do not edit by hand.
description = "Scan open beads for stale trackers that are already implemented — deterministic checks first, LLM verification second"
prompt = """
Generated from Demarch slash-command markdown.

- Source command: /clavain:bead-sweep
- Source file: /home/mk/projects/Demarch/os/Clavain/commands/bead-sweep.md
- When instructions mention Gemini elicitation adapter, ask the user one concise question in chat and wait for the answer.
- When instructions mention activate_skill tool, use Gemini's activate_skill tool.


# Bead Sweep

<sweep_args> #$ARGUMENTS </sweep_args>

**Args:** `--auto-close` (close without asking), `--limit N` (default 50), `plugin-name` (restrict by plugin).

## Step 1: Gather Candidates

```bash
OPEN_BEADS=$(bd list --status=open --json 2>/dev/null) || { echo "Error: bd not available"; exit 1; }
CANDIDATES=$(echo "$OPEN_BEADS" | jq '[.[] | select(.type != "epic")]')
# Optional plugin filter:
# CANDIDATES=$(echo "$CANDIDATES" | jq --arg p "$PLUGIN_FILTER" '[.[] | select(.title | ascii_downcase | contains($p | ascii_downcase))]')
CANDIDATES=$(echo "$CANDIDATES" | jq ".[0:${LIMIT:-50}]")
COUNT=$(echo "$CANDIDATES" | jq 'length')
```

Report: `Scanning ${COUNT} open beads for stale trackers...`

## Step 2: Deterministic Pass

```bash
export DISCOVERY_PROJECT_DIR="."
INTERPHASE_ROOT=$(find ~/.gemini/plugins/cache -path '*/interphase/*/hooks/lib-discovery.sh' 2>/dev/null | head -1)
if [[ -z "$INTERPHASE_ROOT" ]]; then
    INTERPHASE_ROOT=$(find ./interverse/interphase -name 'lib-discovery.sh' -path '*/hooks/*' 2>/dev/null | head -1)
fi
[[ -n "$INTERPHASE_ROOT" ]] && source "$INTERPHASE_ROOT"
```

Call `_discovery_check_implemented "$id" "$title"` per candidate. Bucket results:
- **Confirmed hits**: command file, commit reference, or function found
- **No signal**: nothing found

## Step 3: Report Deterministic Findings

```
## Deterministic Sweep Results

Found N bead(s) with implementation signals:

| Bead | Title | Signal |
|------|-------|--------|
| iv-xxx | /foo command | command file exists: foo.md |
| iv-yyy | Add _bar_func | function exists in: lib-bar.sh |
| iv-zzz | Fix widget bug | referenced in commit: abc1234 |
```

## Step 4: LLM Verification

For each deterministic hit:
1. `bd show <id>` — read bead description
2. Read the evidence file
3. Classify:
   - **Done**: implementation covers acceptance criteria → close
   - **Partial**: some work done, criteria incomplete → skip, note gaps
   - **False positive**: code exists but doesn't address this bead → skip

## Step 5: Close or Report

- **Done + `--auto-close`**: `bd close <id> --reason="Bead sweep: <evidence summary>"`
- **Done, no flag**: ask per bead via Gemini elicitation adapter
- **Partial**: report what's missing

```
## Sweep Summary
- Scanned: {total}
- Signals detected: {signal_count}
- Verified done: {done_count} (closed)
- Partial: {partial_count} (needs review)
- False positive: {fp_count}
- No signal: {no_signal_count} (skipped)
```

## Step 6: Backup

```bash
bd backup 2>/dev/null || true
```
"""
