# Generated by scripts/gen-gemini-commands.sh. Do not edit by hand.
description = "Fix incorrect SKILL.md files when a skill has wrong instructions or outdated API references"
prompt = """
Generated from Demarch slash-command markdown.

- Source command: /clavain:heal-skill
- Source file: os/Clavain/commands/heal-skill.md
- When instructions mention Gemini elicitation adapter, ask the user one concise question in chat and wait for the answer.
- When instructions mention activate_skill tool, use Gemini's activate_skill tool.


<objective>
Update a skill's SKILL.md and related files based on corrections found during execution. Detect skill, reflect on failure, propose fixes, get approval, apply.
</objective>

<context>
Skill detection: !`ls -1 ./skills/*/SKILL.md | head -5`
</context>

## Step 1: Detect Skill

Identify from conversation context (invocation messages, recent SKILL.md references). Set `SKILL_NAME` and `SKILL_DIR=./skills/$SKILL_NAME`. Ask if unclear.

## Step 2: Reflect and Analyze

Focus on `$ARGUMENTS` if provided, else broader context. Determine:
- **What was wrong**: quote specific SKILL.md sections
- **Discovery method**: Context7, error messages, trial/error, docs
- **Root cause**: outdated API, wrong params, missing context
- **Scope**: single section or multi-file?
- **Proposed fix**: which files, which sections, before/after

## Step 3: Scan Affected Files

```bash
ls -la $SKILL_DIR/
ls -la $SKILL_DIR/references/ 2>/dev/null
ls -la $SKILL_DIR/scripts/ 2>/dev/null
```

## Step 4: Present Proposed Changes

```
Skill: [skill-name]
Issue: [1-2 sentences]
Root cause: [brief]

Files to modify:
- [ ] SKILL.md
- [ ] references/[file].md  (if applicable)
- [ ] scripts/[file].py     (if applicable)

### Change 1: SKILL.md — [Section]
Location: Line X

Current: [exact text]
Corrected: [new text]
Reason: [why this fixes it]

[repeat per change]

Impact: [what's affected]
Verification: these changes prevent [specific error]
```

## Step 5: Request Approval

```
Apply these changes?
1. Yes, apply and commit
2. Apply, don't commit
3. Revise (I'll provide feedback)
4. Cancel

Choose (1-4):
```

**Wait for response. Do not proceed without approval.**

## Step 6: Apply

After approval (1 or 2): Edit each file, read back modified sections to verify. If option 1, commit with structured message. Confirm with file list.
"""
