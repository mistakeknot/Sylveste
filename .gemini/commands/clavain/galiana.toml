# Generated by scripts/gen-gemini-commands.sh. Do not edit by hand.
description = "Discipline analytics — view KPIs, report defects, or reset cache"
prompt = """
Generated from Demarch slash-command markdown.

- Source command: /clavain:galiana
- Source file: os/Clavain/commands/galiana.md
- When instructions mention Gemini elicitation adapter, ask the user one concise question in chat and wait for the answer.
- When instructions mention activate_skill tool, use Gemini's activate_skill tool.


# Galiana Command

<subcommand> #$ARGUMENTS </subcommand>

Route by subcommand:

1. **No args:** invoke `Skill("galiana")` to render discipline analytics.
2. **`report-defect <bead-id>`:** collect defect metadata and log it.
3. **`experiment [--date YYYY-MM-DD] [--topologies T2,T4] [--dry-run]`:** run topology shadow experiments.
4. **`eval [--topologies T2,T4,T6,T8] [--fixtures PATTERN] [--dry-run] [--no-interbench]`:** run property-based agent eval harness.
5. **`reset`:** delete KPI cache.

## report-defect

Require bead-id — print usage and stop if missing. Ask user for: `defect_type` (logic/regression/integration/perf/security), `severity` (P0–P4), `escaped_gate` (review/testing/staging/none), `reviewed_agents`, `missed_agents`.

```bash
GALIANA_LIB=$(find ~/.gemini/plugins/cache -path '*/clavain/*/galiana/lib-galiana.sh' 2>/dev/null | head -1)
[[ -z "$GALIANA_LIB" ]] && GALIANA_LIB=$(find ~/projects -path '*/os/clavain/galiana/lib-galiana.sh' 2>/dev/null | head -1)
source "$GALIANA_LIB"
galiana_log_defect "$BEAD_ID" "$DEFECT_TYPE" "$SEVERITY" "$ESCAPED_GATE" "$REVIEWED_AGENTS" "$MISSED_AGENTS"
```

## experiment

```bash
EXPERIMENT_SCRIPT=$(find ~/.gemini/plugins/cache -path '*/clavain/*/galiana/experiment.py' 2>/dev/null | head -1)
[[ -z "$EXPERIMENT_SCRIPT" ]] && EXPERIMENT_SCRIPT=$(find ~/projects -path '*/os/clavain/galiana/experiment.py' 2>/dev/null | head -1)
python3 "$EXPERIMENT_SCRIPT" $FLAGS
```

If not `--dry-run`, read `~/.clavain/topology-results.jsonl` and present summary table (Task/Type/T2/T4/T6/T8 recall values). Offer LLM synthesis.

## eval

```bash
EVAL_SCRIPT=$(find ~/.gemini/plugins/cache -path '*/clavain/*/galiana/eval.py' 2>/dev/null | head -1)
[[ -z "$EVAL_SCRIPT" ]] && EVAL_SCRIPT=$(find ~/projects -path '*/os/clavain/galiana/eval.py' 2>/dev/null | head -1)
python3 "$EVAL_SCRIPT" $FLAGS
```

Present output summary. Highlight property failures (exit 1) prominently; flag regressions (exit 2). If not `--dry-run`, read `~/.clavain/eval-results.jsonl` for fixture-level detail.

## reset

```bash
rm -f ~/.clavain/galiana-kpis.json
echo "Galiana KPI cache reset. Re-run /clavain:galiana to regenerate."
```
"""
