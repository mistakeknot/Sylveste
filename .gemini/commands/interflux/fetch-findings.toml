# Generated by scripts/gen-gemini-commands.sh. Do not edit by hand.
description = "Fetch peer findings from a flux-drive review session. Use to inspect what findings agents have shared during a parallel review."
prompt = """
Generated from Demarch slash-command markdown.

- Source command: /interflux:fetch-findings
- Source file: /home/mk/projects/Demarch/interverse/interflux/commands/fetch-findings.md
- When instructions mention Gemini elicitation adapter, ask the user one concise question in chat and wait for the answer.
- When instructions mention activate_skill tool, use Gemini's activate_skill tool.


# Fetch Peer Findings

Retrieve findings from a flux-drive intermediate findings file.

## Usage

`/interflux:fetch-findings <output_dir> [--severity blocking|notable|all]`

## Execution

1. Parse arguments:
   - `output_dir` (required): The flux-drive output directory
   - `severity` (optional, default "all"): Filter by severity level

2. Run the helper:
   ```bash
   bash ${CLAUDE_PLUGIN_ROOT}/scripts/findings-helper.sh read "{output_dir}/peer-findings.jsonl" {severity}
   ```

3. Parse the JSON output and present in a readable table:
   ```
   ## Peer Findings ({count} total)

   | Time | Agent | Severity | Category | Summary |
   |------|-------|----------|----------|---------|
   | ... | ... | ... | ... | ... |
   ```

4. If no findings exist, report: "No peer findings shared yet."
"""
